# Square Stern rooftop vents

- Reworked the four oversized rooftop units on Stern's red 40 West 4th Street mass to match the requested simplified silhouette.
- Each unit is now a neutral-gray 10-by-10 square housing with a smaller black square block protruding from its top.
- Removed the previous circular fan wells and separate radial fan blades.
- Kept all rooftop geometry inside the existing Stern detail group so it is baked with both CityGML masses into one selectable interaction mesh.
- Verified with `npm run lint` and `npm run build`; both pass.
