# Courant forecourt garden

- Added a narrow raised planting bed in the clear diagonal forecourt beside Courant, outside verified building footprints and roadbeds.
- Populated it with seven staggered mature low-poly trees using instanced trunks and crowns, matching the dense Mercer Street garden reference while preserving the miniature style.
- Restored Lipton Hall's missing interactive palette entry, which had caused scene initialization to fail before any map geometry could render.
- Verified with `npm run lint` and `npm run build`; both pass.
