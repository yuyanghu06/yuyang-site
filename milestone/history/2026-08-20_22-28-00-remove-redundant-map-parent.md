# Remove redundant map parent wrapper

- Moved the shared parent implementation back to `src/components/map.tsx`.
- Removed `src/components/map/map-parent.tsx`; the `map/` directory now contains only focused implementation modules.
- Updated repository guides and current milestone wording.
- Verified with targeted TypeScript and ESLint checks.
