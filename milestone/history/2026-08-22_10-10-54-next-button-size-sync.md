# Next button size sync

Fullscreen blue Next controls now share the minimized choice pill's minimum width, padding, font size, and font weight. Their placement remains outside the message card in fullscreen mode.

Targeted ESLint, TypeScript, and diff checks passed.
