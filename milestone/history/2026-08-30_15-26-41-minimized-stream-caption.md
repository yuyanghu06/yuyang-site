# Minimized stream caption continuity

Fixed the avatar shell so a response that starts streaming while expanded remains visibly rendered after the window finishes minimizing. The docked caption now derives visibility from the active stream and settled dock state while continuing to respect manual dismissal.

Targeted ESLint and full TypeScript validation pass.
