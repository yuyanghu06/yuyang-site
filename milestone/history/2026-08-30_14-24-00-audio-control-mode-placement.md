# Audio control mode placement

- Made the docked audio-control row span the complete avatar tile width so its two buttons center exactly beneath the avatar.
- Positioned the audio controls at the top-right inside the expanded avatar modal.
- Hid the fixed social-profile controls whenever the avatar modal is expanded and restored them in docked mode.
- Targeted ESLint and whitespace validation pass.
