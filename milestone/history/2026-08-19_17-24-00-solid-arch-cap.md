# Solid arch cap

- Identified the apparently transparent upper arch as missing backfaces from reversed/single-sided source normals.
- Set the runtime warm-white arch material to `DoubleSide` so the cap remains solid from every viewing angle.
- Changes remain local and were not committed or pushed.
