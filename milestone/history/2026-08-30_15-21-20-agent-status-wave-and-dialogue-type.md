# Agent status wave and dialogue type

- Rendered every character of `Thinking…` and `Remembering…` as an independently staggered wave step.
- Preserved each status as one accessible announcement and disabled its animation under `prefers-reduced-motion`.
- Removed the visitor bubble's smaller font override so visitor and assistant message text use the same responsive size.
- Targeted ESLint, full TypeScript validation, and whitespace checks pass.
