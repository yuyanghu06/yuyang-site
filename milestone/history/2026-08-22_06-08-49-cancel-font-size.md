# Cancel font size

The inline-composer Cancel action now uses explicit inherited family, `0.72rem` size, 550 weight, and unit line height, exactly matching Reply. This prevents the generic composer-button rule from rendering Cancel at `1.2rem`.

This was a targeted CSS-only correction. A full production build was intentionally not run because no commit was requested.
