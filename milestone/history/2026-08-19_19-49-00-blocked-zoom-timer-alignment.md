# Blocked zoom timer alignment

- Bound the wheel gesture settle timer directly to the 260 ms blocked-zoom animation duration.
- The input lock now expires exactly when the visual zoom-in/return pulse finishes.
