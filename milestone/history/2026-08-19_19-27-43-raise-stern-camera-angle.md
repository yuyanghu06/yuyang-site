# Raise Stern camera angle

- Increased only Stern's selected camera height by 20%, from `210` to `252`.
- Preserved Stern's southwest-facing azimuth, target, radius, and `2.41x` zoom.
- Left Courant, Bobst, and Lipton presets unchanged.
- Verified with `npm run lint` and `npm run build`; both pass.
