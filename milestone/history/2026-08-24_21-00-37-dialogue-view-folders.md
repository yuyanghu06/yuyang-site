# Dialogue view folders

- Moved the Washington Square overview and all five Washington-area dialogue files out of `manhattan/` into a dedicated `washington-square/` folder.
- Left only the Manhattan overview script in `manhattan/` and updated imports without changing runtime narration.
- Added an empty, unwired base `union-square.md` placeholder alongside the two Union landmark placeholders.
- Verified targeted ESLint, full TypeScript checking, and `git diff --check`.
