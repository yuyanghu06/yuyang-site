# Align pill Reply

Reply actions inside caption pill rows now remove their standalone full-width and vertical padding so the Reply text is vertically centered against the adjacent pill label.

This was a targeted CSS-only presentation correction. A full production build was intentionally not run because no commit was requested.
