# Faster empty-map zoom pulse

- Increased the empty-map rejection pulse speed by 50%, reducing its duration from 1,000 ms to 667 ms.
- Preserved the single zoom-in followed immediately by a return to overview and the one-pulse-per-gesture latch.
