# Exclusive selected landmark glow

- The selected building retains its animated violet/cyan pulse during transition and close view.
- Every non-selected landmark glow fades to zero opacity until Escape or scroll-out clears selection, then resumes automatically.
- `npm run lint` and `npm run build` pass.
