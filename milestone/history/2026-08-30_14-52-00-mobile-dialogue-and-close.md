# Mobile dialogue and close control

- Replaced the narrow expanded chat's collapsing auto height with `min(18rem, 42dvh)`.
- Allowed the internal message list to shrink and scroll so wrapped captions and controls remain rendered.
- Raised the expanded close button above the chat layer so it remains visible and clickable on mobile.
- Whitespace validation passes.
