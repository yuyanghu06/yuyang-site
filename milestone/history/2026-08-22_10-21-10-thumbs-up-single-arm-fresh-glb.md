# Thumbs-up single-arm correction and fresh GLB review

- A side-label diagnostic applied the relaxed-arm correction to `RightArm`; the resulting render raised a second fist and proved that the source gesture's single raised chain is `LeftArm`/`LeftHand`.
- Restored the correction to that single raised chain: relaxed `LeftArm`, upward-bent `LeftForeArm`, and direction-mapped left palm/digits. The opposite arm remains untouched.
- Regenerated `public/animations/thumbs-up-review.glb` with one `Thumbs_Up` Action, zero lower-body motion, and exact `0.0` bookend closure.
- Opened the GLB in a new Blender application instance so review cannot accidentally continue in a stale previously imported scene.
