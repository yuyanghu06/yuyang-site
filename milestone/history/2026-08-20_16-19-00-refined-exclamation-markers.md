# Refined exclamation destination markers

- Shortened the bar by roughly 22%, widened it, reduced the outline to three SVG pixels, and shifted the fill to muted ochre.
- Added a soft pale-blue SVG halo and matching fallback shadow.
- Reduced overall marker display size by roughly 13% while doubling bob amplitude at the existing speed.
- Kept both markers generic rather than adding Washington- or Union-specific symbols.
