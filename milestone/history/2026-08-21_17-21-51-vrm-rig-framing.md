# VRM rig and framing correction

- Generated `public/models/yuyang-avatar-vrm1-review.glb` from Tripo's direct fitted 52-bone rig with a complete VRM 1.0 humanoid declaration and standardized body/finger names.
- Added `@pixiv/three-vrm` 3.5.3 and copied the minimum MIT-licensed retargeter from the separately cloned VRM Game Starter.
- Connected the Quaternius-derived `Idle_Loop` through the upstream normalized VRM retargeting path.
- Diagnosed the first site screenshot: the new one-meter asset inherited the previous rig's fixed camera center and absolute facial coordinates, placing the body below frame and the face above it.
- Replaced those assumptions with GLB-bound-derived camera framing and avatar-relative face placement.
- Targeted ESLint and full TypeScript checking pass. Browser re-verification was intentionally skipped per user direction; future validation should inspect the GLB directly unless explicitly requested otherwise.
