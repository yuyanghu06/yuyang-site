# Meshy API configuration documented

- Confirmed that the local environment exposes `MESHY_API_KEY` for authenticated Meshy API work.
- Recorded only the variable name and safe handling policy; the secret value was not inspected, printed, logged, or copied into the repository.
- Future Meshy automation must read the key from the environment at runtime.
- No Meshy generation or regeneration task was submitted as part of this documentation change.
