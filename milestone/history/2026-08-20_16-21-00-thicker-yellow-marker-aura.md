# Thicker yellow marker aura

- Reworked the SVG glow into layered near and outer yellow blooms with expanded filter bounds.
- Increased the fallback shadow blur from 16 to 30 pixels so the pre-decode marker matches the thicker aura.
