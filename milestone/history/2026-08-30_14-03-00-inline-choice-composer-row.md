# Inline minimized choice composer

- Moved the initial minimized Reply composer into the same action row as the dynamically rendered Yes/No controls.
- Kept choice buttons at their intrinsic size and allowed the input form to flex into the remaining caption width.
- Prevented the generic composer branch from rendering a duplicate form for this state.
- Targeted ESLint and whitespace validation pass.
