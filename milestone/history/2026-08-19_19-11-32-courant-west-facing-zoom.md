# Courant west-facing zoom

- Replaced Courant's shared south/front selection azimuth with its own west-facing view at `Math.PI / 2`.
- Left Stern, Bobst, and Lipton camera presets unchanged.
- Verified with `npm run lint` and `npm run build`; both pass.
