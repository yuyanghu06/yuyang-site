# Restore balanced avatar border

- Restored the transparent expanded-call outer layer so the map remains visible around the modal.
- Removed the attempted right-side glass extension.
- Restored the centered modal's original rounded clipping and shadow, producing equal visible margins without changing the avatar renderer size.
- No global background fill or filter remains.
