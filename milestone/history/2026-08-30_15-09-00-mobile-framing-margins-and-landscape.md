# Mobile framing, margins, and landscape dialogue

- Shifted the independent mobile docked avatar from `-50%` to `-72%` and expanded avatar from `-72%` to `-88%` without changing desktop.
- Increased expanded mobile modal margins to 1.25 rem on every viewport edge.
- Allowed landscape dialogue to use the full remaining viewport height below top controls instead of limiting it to 42 d.v.h.
- Whitespace validation passes.
