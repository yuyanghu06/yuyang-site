# Cancel text action

The inline composer's Cancel control is now presented as plain blue text matching Reply instead of a white pill. Its label is non-shrinking and single-line, with no background, border, fixed pill width, or circular button geometry.

This was a targeted CSS-only presentation correction. A full production build was intentionally not run because no commit was requested.
