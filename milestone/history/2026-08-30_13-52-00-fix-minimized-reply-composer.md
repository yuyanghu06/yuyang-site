# Fix minimized Reply composer

- Fixed the free-dialogue Back-state visibility gate that hid both the action row and inline composer after clicking Reply in minimized mode.
- Composer-open state now takes precedence, so Reply consistently replaces the action controls with the inline text box.
- Targeted ESLint and whitespace validation pass.
