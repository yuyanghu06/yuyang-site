# Union Square dialogue placeholders

- Added empty Markdown authoring files for the 25 Union Square West and 235 Park Avenue South camera views.
- Left both files intentionally unwired until their final narration is supplied.
- Confirmed that production loader testing uses Next.js production mode through `npm run build && npm start`; the loader gate reads `process.env.NODE_ENV`.
