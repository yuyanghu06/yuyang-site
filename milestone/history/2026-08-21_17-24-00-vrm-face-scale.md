# VRM face scale correction

- Confirmed from user review that the bounds-derived framing and head attachment now place the complete VRM avatar correctly.
- Reduced the head-relative face canvas from `0.205 × 0.255` to `0.15 × 0.186` times avatar height because the first standardized-rig pass overwhelmed the narrower head.
- Confirmed the site loads `Idle_Loop` from the separate Quaternius-derived library, retargets it through the VRM humanoid mapping, and plays it with Three.js `AnimationMixer`; the rig-only GLB itself remains animation-free.
- Targeted ESLint and full TypeScript checking pass.
