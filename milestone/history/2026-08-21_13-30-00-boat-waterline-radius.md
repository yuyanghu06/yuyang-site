# Boat waterline radius

The globe GLB is normalized to its outer land bounds, leaving its actual water mesh at roughly radius `345` while the nominal landmark radius remains `360`. Moved all boat routes from radius `358` to `344`, seating their lower hulls just inside the measured ocean surface instead of leaving a visible gap. Boat scale and speed remain unchanged.
