# Narrow avatar right-edge correction

- Removed the `2.5rem` right inset from the expanded avatar modal at viewport widths of 600 px and below.
- The narrow fullscreen avatar surface now reaches both horizontal viewport edges instead of revealing the underlying black map canvas as a full-height strip.
- Preserved the existing desktop right margin and all docked-avatar positioning.
- Updated the containing folder documentation and the current milestone state.
