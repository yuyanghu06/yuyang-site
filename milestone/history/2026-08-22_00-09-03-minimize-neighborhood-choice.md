# Minimize neighborhood choice

Selecting either `Union Square` or `Washington Square` from the Manhattan guided-tour caption now minimizes the avatar guide immediately before dispatching the authored neighborhood camera navigation.

Verification completed with targeted ESLint for `src/components/agent-chat.tsx` and `npx tsc --noEmit`. A full production build was intentionally not run because no commit was requested.
