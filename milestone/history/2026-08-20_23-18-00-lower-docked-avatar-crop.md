# Lower docked avatar crop

- Lowered the fixed-scale avatar canvas within the minimized card by changing its vertical translation from `-30%` to `-24%`.
- This preserves the existing character scale and camera while bringing the complete head and hair inside the clipped dock frame.
