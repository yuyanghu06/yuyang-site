# Deduplicate expanded Reply

- Prevented the expanded phase-specific Reply branch from rendering when terminal presentation state already owns Reply.
- Ensured the latest caption displays at most one Reply trigger on mobile and desktop.
- Targeted ESLint and whitespace validation pass.
