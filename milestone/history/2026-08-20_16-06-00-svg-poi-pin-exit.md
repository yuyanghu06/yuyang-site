# SVG point-of-interest pins and exit motion

- Replaced the runtime canvas markers with separate Washington and Union SVG placeholder assets.
- Removed the remaining center dot and reduced the outline from 14 to nine source units.
- Centered the pin tips on the measured park bounds and applied a half-screen-pixel rightward offset.
- Added a 720 ms upward exit when entering either neighborhood so the markers travel out of the screen during the zoom.
