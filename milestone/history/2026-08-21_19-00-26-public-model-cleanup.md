# Public model cleanup

Removed unused and superseded GLB/VRM files from `public/models/`, including rejected avatar retarget diagnostics, intermediate recovery rigs, mesh and rig comparison exports, and the unused watercolor globe web export.

Retained all active map models, generated Washington city tiles, the two authoritative 54-bone V2 avatar exports, the live embedded-idle avatar, both Quaternius source/reference assets, and the explicitly protected torso-corrected avatar mapping checkpoint.

Rewrote `public/models/info.md` as the authoritative inventory of the remaining public model assets. No application code changed.
