# Rename final response action to Done

Renamed the completed-response minimize action from `Cancel` to `Done` in docked and expanded layouts. Expanded final responses keep `Reply…` inside the message bubble, matching the intro treatment, while `Done` remains the separate action below. The composer dismissal remains labeled `Cancel`.

Targeted ESLint and full TypeScript verification passed.
