# Pinch-only mobile map zoom

Removed one-finger vertical swipe zoom from the shared map input controller. Mobile zoom navigation now requires a two-finger pinch at every map level, while one-finger globe/neighborhood dragging, taps, desktop wheel zoom, and the existing pinch thresholds remain intact.
