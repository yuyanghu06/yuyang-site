# Current Project Milestone

## Tour segment control priority (2026-08-30)

Authored tour dialogue with unseen caption segments now shows one Next control and nothing else. Destination choices, tour navigation, Back, and Reply appear only after the final segment of that stop has been revealed, preventing terminal hard-coded controls from competing with dialogue continuation.

## Shift memory refresh and reusable Pinecone skill (2026-08-30)

The canonical Google Doc's existing `organization:shift` record now includes a dedicated company overview and funding subsection. It records Shift/microagi as a German robotics startup focused on collecting and structuring real-world human and industrial demonstration data for training and fine-tuning industrial robot policies, Yuyang's summer 2026 Founders Associate role, and the $55 million seed round described by the company and contemporary reporting as Germany's largest seed round as of August 2026.

Google Doc revision `AIroW378mZgk1mequyw_9SFHQTV0I4tAiBbe6vX3nhxU8GJ0jJM1fnxFC6jI6Aagkt9CqxNyZixuAu8aZD3rrBex0zizXXiBnJr7y4A6zw` produced 166 chunks. The confirmed `memories` / `__default__` Pinecone namespace was replacement-ingested and verified at exactly 166 records. Exact entity, semantic paraphrase, multi-turn follow-up, authoritative base-fact bypass, and unsupported-question uncertainty checks all passed without exposing retrieval details.

The repository now includes `.codex/skills/pinecone-memory/`, a validated custom Codex skill for the complete canonical-Doc authoring, temporary export, deterministic build, confirmed destructive replacement, retrieval verification, cleanup, documentation, and handoff workflow. Generated chunks now retain `sourceRevisionId` directly, and ingestion forwards that per-chunk value into Pinecone metadata.

## Tavily Researching status (2026-08-30)

When the model calls the private Tavily web-search tool, the pending dialogue card now switches from `Thinking…` to the animated `Researching…` label for the duration of the search, then returns to `Thinking…` while the grounded answer is composed. Mixed private-memory and web-search rounds update the same card to match each server tool as it runs.

## Dialogue-only agent responses (2026-08-30)

The base prompt now forbids bullets, numbered lists, headings, dash-led items, and checklist formatting in every visitor-facing answer. Multi-fact and web-search answers must still read as natural conversational paragraphs.

## Model-initiated Tavily web search (2026-08-30)

The portfolio agent now exposes a private server-side `web_search` function backed by Tavily. The Responses API keeps tool choice on `auto`, but the prompt requires a Tavily call before answering anything that needs live or up-to-date data. The model may also search whenever it judges that an answer needs niche, uncertain, or externally verifiable public information, as well as when a visitor explicitly asks it to look something up.

Search arguments are independently validated, queries are limited to 500 characters, requests time out after 12 seconds, and returned answers and result snippets are bounded before they re-enter model context. The prompt keeps personal facts routed through Pinecone memory, allows combining both tools when necessary, treats web results as untrusted evidence, and asks the model to link the useful source pages naturally without exposing Tavily or internal tool mechanics. The configured Tavily credential passed a live search smoke test; targeted ESLint, full TypeScript validation, and whitespace validation pass.

## Safe audio fade interpolation (2026-08-30)

The shared audio fade primitive now clamps animation progress and every interpolated volume frame. Floating-point overshoot at the end of exponential fade-outs can therefore no longer assign a tiny negative value to `HTMLMediaElement.volume` and interrupt audio transitions with a browser exception.

## Inline dialogue controls and docked replies (2026-08-30)

Phase choices, Next/Back/Done actions, and the single Reply trigger now render inside the latest assistant message bubble in both expanded and docked layouts. This removes the detached control spacing and prevents generic expanded Reply rendering from duplicating phase-specific Reply controls.

The chat boundary now stops keyboard events as well as pointer clicks before they reach the minimized avatar shell. Pressing Enter or Space while using the docked inline composer therefore no longer expands the avatar window. Mid-dialogue submission still discards unseen queued output and sends only the displayed assistant segments as context.

The intro-choice composer exception is limited to the active Yes/No state. If a visitor replies from that state and receives an agent answer, pressing Reply on the answer now replaces the action row with the visible inline textbox instead of hiding both controls.

Submitting any inline reply now exits the originating scripted tour or landmark branch, clears its navigation history and unseen caption queue, and starts a freeform model response. Therefore an old scripted Next action cannot resume after the visitor branches; any new Next action belongs only to segmentation of the generated answer.

## Settled social-control reveal (2026-08-30)

The GitHub, LinkedIn, and Instagram controls now remain invisible and non-interactive throughout the avatar's 460 ms minimize transition. They fade in over 220 ms only after the docked tile reaches its settled geometry, matching the existing audio-control reveal timing.

## Minimized live-stream caption continuity (2026-08-30)

Minimizing the avatar after assistant text has already begun streaming now keeps the live response visible and updating beside the settled docked tile. Docked visibility is derived from the active-stream and settled-dock states, rather than depending on the stream-start callback firing again after minimization. Manual caption dismissal remains authoritative.

## Animated agent status and matched dialogue type (2026-08-30)

The pending `Thinking…` and `Remembering…` labels now animate with a subtle staggered wave across every character instead of rendering as an unanimated text node. The animation honors `prefers-reduced-motion`, and each complete label remains available to assistive technology as one status announcement.

Visitor bubbles now inherit the same responsive font size as assistant captions. Their distinct alignment, color, background, and compact padding remain unchanged.

## Five-second rolling world-loader log (2026-08-30)

The production world loader now remains active for five seconds, and the complete centered composition is scaled up by 30%. Both the centered three-line log and ready-state CTA use a 2.5-rem title offset for consistent spacing across both loader states. The log progressively reports locations, stories, memories, experiences, communities, landmarks, and conversations before finishing the world. The formerly invisible white-on-white heading was removed. Activating the dark `Dive in!` CTA begins audio priming immediately, fades the complete white loader away over 360 ms, and only then removes it.

## Natural agent answers and 120-character renderer segmentation (2026-08-30)

The dialogue renderer uses one 120-character soft target across every viewport. It prefers a nearby paragraph or punctuation boundary, queues overflow, and reveals one segment per manual blue Next action. Caption length remains entirely a renderer concern.

The base prompt no longer tells the model to write to a character limit. It instead requires one coherent point, a few relevant supporting details, natural transitions, and no unrelated fact inventory. Broad introductions should connect two or three themes rather than reciting every known fact. Ten genuine user messages remain as cadence evidence, without copying their typos or abbreviations.

Every OpenAI Responses round now emits one structured Vercel log entry containing the latest visitor message, that round's model output, and minimal request/round/model identifiers. It intentionally excludes the system prompt, retrieved memory, prior conversation, and full context window.

## Seamless world-loader reversal (2026-08-30)

The production entry globe's continent drift now alternates direction at both animation endpoints. It travels forward, reverses at the end, and reverses again at the start instead of visibly snapping back to its initial frame. The independently rotating satellite orbit remains unchanged.

## Centralized feature styles and shared liquid glass (2026-08-30)

All CSS now lives under `src/styles/`. The former 1,343-line App Router global sheet is split into a 50-line global base plus dedicated social-link, map-shell, world-loader, avatar-call, and agent-chat stylesheets imported by their owning route or component. The avatar shell and chat are separate files rather than one relocated monolith.

The dark liquid-glass surface is now a single global primitive shared by assistant message bubbles, audio buttons, and social buttons. Agent-chat no longer duplicates its background, border, shadow, or backdrop-filter declarations. All six stylesheets parse successfully; targeted ESLint and full TypeScript validation pass.

## Mobile dialogue regression cleanup (2026-08-30)

The temporary JavaScript height synchronization and nine-rem multiline minimum were removed after they stretched both expanded and docked cards. Dialogue cards again use intrinsic content height in every mode. The actual minimized crop came from the generic mobile chat rule's `height: 45%` leaking into docked mode; the docked override now resets height to `auto`. Its width formula reserves an exact 1.25-rem viewport-edge inset, and the same rules cover short-landscape layouts up to 900 px wide.

Docked mobile and short-landscape choice pills render at 75% of their original dimensions, including font size, padding, minimum width, and inter-button gap. Expanded and desktop controls are unchanged.

Assistant dialogue cards clip the shared liquid-glass paint to their rounded boundary and override the generic broad outer surface shadow with the original inset highlight. This removes the square shadow plate that appeared behind rounded dialogue divisions after the stylesheet extraction.

Short-landscape avatar spacing is content-aware rather than a fixed viewport percentage. The dialogue renderer measures the bottom edge of the current visible card and direct action controls after text, phase, pending, composer, or presentation changes; it publishes that coordinate with a 12 px gap. CSS clamps the avatar anchor between 32% and 48% to retain safe framing while following variable dialogue height.

The dynamic-spacing layout effect cleanup uses an explicit void-returning block, satisfying React's `EffectCallback` contract instead of leaking the string returned by `removeProperty()`.

## Desktop docked audio-control sizing (2026-08-30)

The docked effects and music controls now match the desktop social controls exactly: 2.65-rem circles with 0.875-rem icons. A fixed square flex basis and one-to-one aspect ratio prevent the buttons from compressing into vertical ovals. Mobile docked sizing and the separate 1.25-rem expanded controls remain unchanged.

## Agent layout, JSON registries, links, and RAG authoring policy (2026-08-30)

`src/agent/` is now divided by responsibility: `context/` owns the Markdown base prompt plus JSON camera/link registries, `contracts/` owns shared stream and browser-event types, `memory/` owns Pinecone retrieval, and `runtime/` owns orchestration and tool validation. Camera configuration is data-only in `camera-views.json`; the TypeScript companion exposes typed accessors. All model-callable tools now live in one `agent_tools.json` collection with no internal/external schema split.

The new server-executed `hyperlink` tool resolves only six approved keys from `context/links.json`: GitHub, LinkedIn, Instagram, Tech@NYU, BAC, and Shift. Arbitrary URLs are rejected, explicit profile/link requests force this lookup on the first model round, and the model renders the returned URL naturally without exposing the registry. The Shift base context now also covers growth marketing and the negative-CAC data-collection funnel.

An independent all-tool audit verified navigation, emotes, RAG, multi-turn retrieval, base-fact bypass, uncertainty, link lookup, stream privacy, and invalid API requests. It found and prompted fixes for two edge cases: the bounded loop now permits all six approved links to be resolved before the final answer, and runtime parsers independently reject extra argument properties instead of relying only on strict model schemas.

`AGENTS.md` now defines the canonical memory workflow. The Google Doc is the sole authoring source, Pinecone is a replaceable derived index, direct vector edits are forbidden, and temporary exports/corpora must stay outside the repository. The policy records heading structure, metadata fields, contextual chunking, stable IDs, embedding dimensions, destructive replacement confirmations, cleanup, and retrieval verification requirements.

## Live agent status and hardened visitor voice (2026-08-30)

Pending site-agent replies now display `Thinking…` immediately, switch to `Remembering…` only while the private Pinecone memory search executes, and return to `Thinking…` while the grounded response is composed. These are transient stream states and never become transcript messages.

The base prompt now identifies the speaker explicitly as Yuyang Hu and requires first-person answers about his own experience. Visitor-facing answers must remain casual and finish naturally without career-coach follow-up menus. Retrieval evidence no longer contains numbered/source labels, citation metadata is no longer streamed to the browser, and the prompt forbids exposing citations, IDs, scores, metadata, or other retrieval artifacts.

## Soft-boundary caption splitting (2026-08-30)

The dialogue renderer segments complete messages around the 120-character soft target. Streaming previews expose the first segment while retaining the complete answer for the manual Next queue.

Authored camera dialogue opened outside the guided tour queues manual Next for overflow. Guided-tour controls continue to navigate authored locations after each stop's segments finish.

## Final-response Done action (2026-08-30)

Completed agent responses now use `Done` instead of `Cancel` for the white minimize action. Docked `Reply…` remains beside `Done`; expanded `Reply…` stays inside the final message bubble while `Done` sits below it. The composer-only action remains correctly labeled `Cancel`.

## Pinecone-only dynamic RAG and memory reset (2026-08-30)

The live site agent no longer performs unconditional retrieval. Authoritative base facts answer directly; every other personal question must call the private `search_personal_memory` function before answering, and unsupported facts return an explicit unknown. Private tool calls execute only on the server and are never dispatched as browser commands. Bounded recent conversation plus model-authored standalone queries and entity lists resolve multi-turn references before search.

The private search pipeline classifies each query by memory domain, extracts named entities, embeds a conversation-aware query with `text-embedding-3-small`, and runs three Pinecone searches in parallel: broad dense, metadata-domain-filtered dense, and lexically constrained search over per-vector `searchTerms`. Reciprocal-rank fusion combines candidates with entity and domain boosts. Pinecone is the sole deployed memory store; no source or corpus mirror is checked into the repository.

The `memories` index's `__default__` namespace was explicitly validated, cleared, and rebuilt from Google Doc revision `AIroW37fgTiUkERk989h3bgJUd8RStHrCnGz-ugu1TTLGPChXzKiaG2g1ZG5D88FCjleFP-pSivqLvoVjJHz63U1j1laxKMAPXPtoqPhqQ`. It now contains exactly 166 contextual chunks across 153 records. Temporary source/corpus files were deleted after successful ingestion. Live checks verified base-fact bypass, private personal retrieval, multi-turn pronoun resolution, lexical filtering, and unknown-answer refusal.

## Latest reply-control overlap fix (2026-08-30)

Replying from a retained tour or choice caption no longer renders that caption's old action row alongside the agent response controls. Scripted actions are suppressed while an agent request is pending and whenever the response renderer owns a Next or Done presentation.

Targeted ESLint, full TypeScript, and live-browser verification pass after the concurrent retrieval work removed the temporary local corpus import.

## Latest dialogue-state and transcript polish (2026-08-30)

The avatar's talking mouth now starts only when the first non-whitespace assistant text delta becomes visible. It remains neutral while the agent request is pending or reasoning, pauses for semantic emotes, resumes only when visible response text already exists, and stops at stream completion.

Expanded chat history now includes visitor replies in conversation order as right-aligned light bubbles. The experimental message tails and continuous SVG silhouettes were reverted after they distorted and clipped minimized captions; the original rounded bubble artwork is authoritative again. Docked mode continues to show only the latest assistant caption.

The minimized Reply control now opens the inline text composer in every dialogue phase. Composer-open state takes precedence over both action-row and phase-specific input gates. In the initial choice phase, Yes/No remain fixed on the left while the redundant Reply trigger is replaced by a flexible composer that occupies the remaining width in the same row.

The avatar exposes separate global effects and music mute controls as two circular buttons with minimal monochrome SVG icons. Their row spans the full docked tile width for exact centering below the avatar and moves to the top-right inside the expanded modal. Audio and social controls now match the assistant message bubble directly: dark translucent layered surface, light foreground, subtle border and inset highlight, 22 px blur, saturation, and soft shadow. Docked controls remain invisible for the full 460 ms minimize motion, then fade in after the tile reaches its settled position instead of visibly following the transition. Accessible labels and hover titles retain explicit meanings. Effects covers dialogue blips, traffic/crowd ambience, and globe travel audio; music independently covers the four-track background playlist. Three matching circular SVG logo buttons at the top-left link to Yuyang's GitHub, LinkedIn, and Instagram profiles, and hide while the avatar is expanded.

All five control SVGs are 30% smaller than their initial icon size. Social paths use direct pure-white fill/stroke values, matching the audio icons without relying on link color inheritance or a glow.

Fullscreen audio controls now use the same settled-state reveal policy as docked controls: they remain hidden for the complete 460 ms expansion and fade in over 220 ms only after the modal reaches its final fullscreen geometry.

The rejected fixed 78 × 62.4 rem docked source-plane override and proportional mobile correction were removed because they distorted desktop and could leave the mobile tile blank after live resizing. Desktop is restored exactly to its approved `translate(-59%, -50%) scale(0.34)` crop. Mobile now owns completely separate coordinates: docked `translate(-50%, -49%) scale(0.72)` and expanded `translate(-72%, -18%)`. Social controls continue to hide while a docked caption is visible, preventing narrow-screen overlap with dialogue.

Mobile expanded dialogue no longer uses a collapsing auto-height flex container. It receives `min(18rem, 42dvh)` of explicit height and a `min-height: 0` internal message scroller, allowing complete wrapped captions and controls to render. The modal close button is raised from stacking level 2 to 4 so the later-rendered chat cannot cover it.

The narrow expanded dialogue top offset is 3.25 rem, keeping the caption below the close and audio controls without the earlier excessive five-rem gap. Its dimensions and desktop layout remain unchanged.

Expanded audio buttons now exactly match the close button's 1.25-rem circle size. Their SVGs reduce to 0.65 rem, and mobile aligns both audio controls to the same one-rem top inset as the close control; desktop uses the shared 1.5-rem top inset. Docked audio buttons remain larger.

The avatar renderer now synchronizes its framebuffer and orthographic projection after live window resizes while expanded and after the modal's width transition completes. It temporarily removes the cached render dimensions, measures the responsive CSS host once, re-locks those dimensions, resizes WebGL, and recomputes the projection width from the preserved framed height. This replaces CSS-only attempts to compensate for a stale canvas and avoids reintroducing the rejected host ResizeObserver feedback loop.

Mobile framing receives a second isolated adjustment from visual review: docked translation moves from `-50%` to `-72%`, while expanded translation moves from `-72%` to `-88%`, shifting the right-weighted avatar into the center without changing desktop. The expanded mobile modal now reserves 1.25 rem on all viewport edges, and short landscape dialogue uses up to the complete remaining height below its top controls rather than only 42 d.v.h.

Mobile docked avatar scale reduces from `0.72` to `0.58`, preserving the reviewed horizontal coordinate while adding breathing room around the portrait. Narrow expanded assistant cards now force `width: 100%`, normal whitespace, and anywhere wrapping so their text cannot remain on one max-content line and be paint-clipped in landscape.

Mobile no longer centers the avatar by translating its canvas beyond the render host. The Three.js renderer stores the approved desktop right-offset camera center and a separate mobile center derived from the avatar bounds, switching at 760 px during responsive synchronization. Mobile CSS returns to a centered `-50%` canvas transform, eliminating the vertical edge that cropped the avatar's right side. Mobile assistant cards additionally disable paint containment/overflow clipping. Social controls remain visible throughout docked conversations and hide only while the avatar is expanded.

At 600 px and narrower, the social-control row moves from the desktop top-left position to a fixed 0.75-rem bottom-right inset. It remains visible while docked and follows the existing fullscreen hiding rule.

Expanded chat no longer lets its message list consume all remaining flex height when a direct choice row follows. A relational selector switches the list to content height, placing both choice buttons directly below the caption in desktop and mobile layouts.

The expanded phase-specific Reply branch is suppressed whenever the terminal presentation state already owns Reply. This removes the duplicate Reply triggers observed on mobile and applies identically on desktop.

## Shared site audio controller (2026-08-30)

All browser audio construction and playback lifecycle now live in `src/components/site-audio.ts`. The map runtime reports only its current geographic view, landmark lock/settle state, and globe-transition state to a dedicated controller; `agent-chat.tsx` requests throttled dialogue blips through the same module. No other source file constructs an HTML `Audio` element.

One exported `fadeAudioVolume` primitive now handles every volume envelope with cancellable `requestAnimationFrame` transitions and direction-aware exponential curves. Traffic ambience uses 1.2-second fades between silence, Manhattan `0.27378`, and neighborhood `0.328536`; building crowd audio uses 250 ms fades around its `0.216` landmark-only loop; the `0.1` globe-transition effect uses 200 ms fades at both ends. Superseded fades cancel cleanly, and controller disposal stops/resets every owned audio element.

Four user-provided chiptune instrumentals now form the site-wide background playlist in this order: 4:16 8-bit chiptune, Platform Shoes, Pixel Dreams, and Exploration Chiptune. Playback begins only after the experience-start gesture, stays continuous across globe, city, neighborhood, and landmark views, runs at a deliberately low `0.06` volume, and uses the shared exponential fade function for three-second crossfades before wrapping to the first track. The source MP3s are copied without destructive volume re-encoding.

## Latest web-agent canonical base context (2026-08-30)

The portfolio guide's base system instructions now include a small authoritative personal-facts block: San Diego hometown, 5'11" height, favorite country song ("Springsteen" by Luke Combs), former BAC ML Director status after stepping down following Spring 2026, Sally Hu's corrected name, and a compact Shift contribution summary. The agent uses these facts directly, never exposes them as a Quick Facts section/list, never infers or states Yuyang's current residential base, and dynamically retrieves any other personal knowledge.

The authored 235 Park Avenue South narration tells the complete Shift story in roughly 120-character segments queued behind manual Next controls.

## Latest agent presentation-tool cleanup (2026-08-30)

The portfolio guide no longer exposes `display_blue` or `display_white` to the model. The browser renders each complete streamed answer as one message and selects the final Done state without presentation tool calls or overflow segmentation. The remaining model-callable tools are map navigation and avatar emotes.

The avatar-emote schema now exposes only the three animations backed by production runtime sources: `wave_hello`, `nod_smile`, and `head_shake_disappointed`. The unimplemented `smile_tap`, `thumbs_up`, `point_right`, `sad`, and `thumbs_down` values were removed so the model cannot queue no-op emotes.

## Latest avatar framing repair (2026-08-30)

The avatar renderer and docked transform now match the approved `washington-square-dev` composition again: 225% camera framing, its original right-offset camera, 1.5× pixel-ratio ceiling, and 59% docked horizontal transform. The later resize observer was removed because it fed measured canvas dimensions back into the render host during expanded-to-docked transitions, leaving the avatar outside the minimized tile's clipped viewport.

The continuous far-away city traffic ambience is 30% louder in both supported mixes: Manhattan overview rises from `0.108` to `0.1404`, while Washington/Union zoomed-in views rise from `0.1296` to `0.16848`. Building-only crowd audio and globe transition effects are unchanged.

A follow-up audio adjustment raises both traffic-ambience mixes by another 30%, producing `0.18252` in Manhattan and `0.219024` in Washington/Union. The globe ↔ Manhattan transition effect is independently 30% louder, rising from `0.025` to `0.0325`; its 300 ms fades and routing remain unchanged.

All three recently adjusted audio levels then receive a further 50% increase: Manhattan traffic ambience is `0.27378`, Washington/Union traffic ambience is `0.328536`, and the globe ↔ Manhattan transition effect is `0.04875`. Crowd audio, dialogue audio, routing, and fades remain unchanged.

The globe ↔ Manhattan transition effect receives one additional 30% increase, from `0.04875` to `0.063375`. Its routing and 300 ms fades remain unchanged.

The community people-talking track is 80% louder, rising from `0.12` to `0.216`. It retains its building-only routing: it loops only after an individual Washington/Union landmark zoom settles and stops outside that state.

The globe ↔ Manhattan transition effect is now set directly to `0.1`. Its routing remains unchanged.

The globe-transition effect's fade-in and fade-out are shortened from 300 ms to 50 ms, making the sound audible nearly immediately while retaining a small anti-click envelope.

The ElevenLabs hosted MCP remains installed and OAuth-authorized at the provider's US endpoint. The Sputnik experiment was rejected completely: its runtime loop is removed and all five original/processed/generated repository candidates have been removed from `public/audio/`. The user's source remains outside the repository in Downloads, while a recoverable copy of the removed candidates is in the macOS Trash.

## Latest responsive avatar pass (2026-08-26)

The avatar shell now uses dynamic viewport height units for expanded layouts, a one-rem mobile-safe modal inset, and a resize listener that re-clamps a manually docked tile to the current viewport. The Three.js avatar renderer uses a resize observer and a pixel-ratio ceiling of 2 so responsive changes redraw at the current canvas resolution. Avatar framing is now a prominent 110% scale, and the narrow-screen composition uses a larger centered canvas. Targeted lint passes; mobile and 1920×1080 Chrome viewport screenshots were visually checked.

The follow-up framing adjustment reduced the avatar camera scale from `1.4` to `1.1` after visual review showed excessive empty space around the character on desktop, while retaining portrait centering.

Chrome review at 1050×830 and 390×844 confirmed the larger desktop avatar and the final portrait-only left compensation, which keeps the mobile body inside the modal.

Last updated: 2026-08-24 21:17 PDT

This is the authoritative rolling project state. Historical session handoffs are stored as timestamped, append-only files in `milestone/history/`.

## Milestone status

The uncommitted procedural ambient-audio experiment was reverted. A user-provided community-crowd recording is trimmed to 80 seconds and loops at `0.216` only after an individual building zoom settles, never in neighborhood or Manhattan overview views. A separate 2.168-second user-provided zoom effect triggers only for globe ↔ Manhattan travel, peaks at `0.1`, and uses 50 ms fades; it does not play for park or building zooms. The base Manhattan traffic recording fades in to `0.27378`, stays continuous through park zooms, and rises gradually to `0.328536` in neighborhood views. The existing dialogue blip remains unchanged.

The approved Washington Square visual language now extends from a new globe entry view into the base Manhattan overview. The single persistent renderer starts at globe scale, flies into Manhattan, and retains Washington Square and Union Square as zoomable neighborhood views.

The expanded FaceTime shell uses its original centered bounded dimensions with equal surrounding map margins. Its transparent outer layer, rounded clipping, and shadow are restored, so the background remains visible as an intentional balanced border without any global filter or fill.

The literal expanded modal is centered with `left: 50%` and `translate(-50%, -50%)`. Its desktop maximum width is `82.08rem`, a 10% reduction from the preceding 91.2-rem review. The expanded Three.js camera uses its earlier `0.56 / FULLSCREEN_AVATAR_SCALE` horizontal offset again; vertical framing remains unchanged.

## Current visual direction

- Render Washington Square and its immediate surrounding neighborhood as a restrained 3D miniature.
- Buildings explicitly selected by the user will eventually receive custom recognizable 3D models.
- All other context buildings use the official NYC 2014 CityGML roof, wall, and ground surfaces with a deterministic, higher-depth sun-faded Village palette spanning limestone (`#B9A98D`), sandstone, dusty brick, terracotta, sage, cool gray stone, aged cream, and dark brown-gray. Roofs are lighter and less saturated than their corresponding walls for clearer massing.
- Three verified buildings bypass the procedural palette through BIN-specific overrides: One Fifth Avenue (`1008847`) uses pale weathered gray stone, the NYU Silver Center complex (`1008820`) uses light gray-beige masonry, and Lipton Hall (`1008875`) retains its earlier muted red-brown brick palette. Each has a distinct roof treatment. Lipton additionally carries 447 dark window instances stacked floor-by-floor across 14 eligible CityGML facade faces.
- Washington Square is populated with 88 deterministic low-poly trees using the exact Courant garden trunk/crown geometry and materials. Placement is validated against the authoritative 37-point OpenStreetMap Washington Square Park boundary: 24 probes around each tree's full scaled crown plus a 0.6-meter safety margin must remain inside the park. Trees also maintain clearance from mapped paths, the fountain, the arch, and neighboring trees, so neither trunks nor canopies enter the surrounding roadbeds.
- Roughly 18% of Washington Square trees are deterministically selected as taller specimens, with height scales between 1.45× and 2×; the remaining trees receive subtler scale variation. Tree footprints are included in pedestrian route validation to prevent trunk clipping.
- Washington Square crowns now vary independently in width, depth, and vertical fullness across a 0.68–1.36 base scale range. Five softly varied Apple-inspired sage greens replace the earlier uniform dark canopy while retaining the shared Courant low-poly tree geometry.
- The mapped fountain footprint now drives a simple low-detail 3D fountain at its exact OSM center and diameter: muted limestone base and rim, recessed blue-gray water, and a small central pedestal/bowl.
- The fountain includes eight lower pale-blue arcing sprays plus a tapered central geyser roughly 1.7 times the mapped basin radius in height, matching the tall dominant plume of the real Washington Square fountain. Animated droplets flow continuously along the side arcs.
- The map now loads `public/models/washington-square-arch.glb`, isolated from the existing Blender Sketchfab scene as nine meshes. All imported materials are overridden with warm white, the model is normalized to the mapped 20.5-meter height, and it uses OSM way `248166269` for its exact north-gate position and rotation. A visibly open procedural model remains only as a load-failure fallback.
- CityGML BIN `1088400` is excluded from context massing because its solid block occupied the same footprint and hid the detailed Blender arch.
- The imported arch material renders double-sided because several Sketchfab cap faces use reversed/single-sided normals; this keeps the upper section visually solid from every camera rotation.
- The approved Washington Square Arch asset now uses the original detailed nine-mesh Sketchfab geometry with one pristine flat stone lid joined directly over its real Blender crown opening. It exports as the single upright `WashingtonArchOriginalFlatTop_Final` object; the earlier simplified corrected copy and all runtime cap/axis workarounds have been removed.
- Mapped crosswalks are being rebuilt from the OSM crossing nodes plus measured planimetric asphalt spans. The new renderer uses thinner, closer muted stripes sized to each street, and the extraction now rejects the erroneous crossings several blocks west of Washington Square.
- Crosswalks now come from tagged OSM `highway=crossing` nodes and inherit their orientation from connected footway geometry; all earlier hand-estimated crossing coordinates were removed.
- Park-path rendering now requires every centerline and full-width edge probe to remain inside an actual Washington Square, Union Square, or Gramercy Park polygon while also staying outside official planimetric roadbeds. Five longitudinal samples at the center and both sides retain 919 true park-path segments and remove the out-of-park OSM sidewalks/footways that previously drew raised pale strips around Union Square blocks and intersections.
- Union Square's three open OpenStreetMap lawn-boundary fragments are consolidated into one closed outer park perimeter, eliminating the erroneous diagonal gray wedge while preserving mapped paths and fixtures above a continuous grass surface. Gramercy Park remains a separate closed grass polygon.
- Union Square and Gramercy park-ground materials now use the official roadbed stencil as an exclusion mask, preventing reconstructed grass polygons from clipping over asphalt at the Union Square junctions.
- The shared park-data request version was advanced after regenerating the Union polygon, preventing the browser's one-day JSON cache from serving the earlier malformed diagonal geometry.
- Union Square and Gramercy Park share Washington Square's deterministic low-poly tree language. Sixteen frozen edge placements whose crowns overlapped authoritative roadbeds were explicitly removed from the authored set, leaving 80 trees with zero road-overlapping canopy probes. Generation and validation remain build-time only; the browser reads the frozen array without runtime placement work.
- The downloadable CC BY Sketchfab scene `Washington Square Park v03` by `nyu_grad_alley_2020` is now imported into `assets/blender/nyu-custom-buildings.blend` as the style source for Bobst, Courant, and Stern. Bobst's missing rear wall has been closed with a cleaned mirror of its modeled five-bay front facade, retaining the source masonry, glazing, and opening materials without duplicating side-wall trim.
- Corrected Bobst is exported as the 127 KiB `public/models/bobst-library.glb`, replaces CityGML BIN `1008626`, and is scaled to its authoritative footprint and height. Its yaw follows the long Washington Square South facade edge; a CityGML-only fallback remains for GLB load failure.
- Courant and Stern retain their authoritative CityGML massing instead of receiving separate Blender models. Courant is the narrow BIN `1008627` tower. The grouped Stern selection uses the adjacent white BIN `1078952` and red BIN `1077346` masses. Their CityGML surfaces and procedural facade/roof details are baked into one mesh per interaction target before loading; the background tiles omit those BINs to prevent duplicate geometry. The unrelated BIN `1008629` no longer receives the generated window grid.
- The red/gray Stern palettes are assigned by building identity: 40 West 4th Street uses muted red masonry, while the rotunda-bearing 44–50 West 4th Street mass uses cool gray stone. The rotunda has a curved blue-glass band and a tall purple banner whose top edge attaches to an angled pole and whose full 5.2-unit length hangs directly downward.
- Gould Plaza has a lightweight close-view detail pass with raised paving, five low-poly trees, and nine instanced seats. The complete plaza composition is rotated `-0.57` radians to run parallel with the nearest official street edge, and its paving is reduced from `29×22` to `26×18` so it remains inside the plaza instead of clipping the road. The large rectangular Stern/Berkeley roof has four oversized neutral-gray square HVAC housings, each capped by a smaller protruding black square fan block. True polygon-area ranking anchors them to the broad flat roof instead of the thin parapet surface, and the complete assemblies sit 0.75 scene units above the roof datum for clearer vertical exposure.
- Courant's open east forecourt wedge now contains a narrow raised sage planting bed with seven staggered mature low-poly trees. The earlier west-gap placement was removed; every new tree center is verified outside both CityGML building footprints and official roadbeds.
- The orthographic camera uses a tighter crop and a 30% lower elevation. Horizontal click/touch dragging rotates around Washington Square; pan, wheel zoom, pinch zoom, and alternate view navigation remain disabled.
- Previous generated style-reference images remain non-authoritative. The earlier Manhattan implementation is retired, but Manhattan → Washington Square/Union Square is once again the active view hierarchy using the approved current palette and geometry pipeline.

## Authoritative future loading and caching architecture

- The finished site will use a single persistent Three.js canvas, WebGL renderer, scene, and React component across its geographic hierarchy. Navigation must not remount Three.js or create a new renderer at each level.
- The intended hierarchy is globe/planetary overview → city view such as Manhattan or San Diego → neighborhood view such as Washington Square or Union Square. The exact globe art direction remains open, but this hierarchy is now the basis for asset-loading and caching work.
- The first website startup will replace a conventional blocking loader with an intentional conversational introduction: a small animated avatar of Yuyang connected to his AI talks to the visitor while the persistent renderer initializes, initial shaders warm, and the first complete scene becomes ready. This experience must still veil partial scene geometry; once ready, it hands off into the initial scene animation.
- Every geographic asset group must have three explicit lifecycle states: **downloaded** (bytes in HTTP/browser cache), **prepared** (GLB/data decoded into CPU-side Three.js geometry and materials), and **visible** (objects attached to or revealed in the active scene). Code must not treat a downloaded response as render-ready.
- Versioned GLBs and stable datasets should use durable HTTP caching. Likely next destinations should also be fetched and decoded proactively while the user is viewing the current level; nearby prepared destinations may remain in memory, while distant geometry may be detached/disposed according to a memory budget without invalidating its browser-cached bytes.
- Camera zoom/flight animations are the primary loading window after startup. A globe-to-city flight prepares the city; a city-to-neighborhood flight prepares the selected neighborhood. Hover, selection, scroll intent, and current camera destination should reprioritize the queue before arrival.
- Subsequent geographic transitions must not show another blocking loading screen. If a camera flight reaches its destination before preparation completes, extend or soften the final approach and reveal remaining tiles progressively rather than interrupting the experience with a loader.
- Network fetching, CPU decoding/preparation, GPU attachment, and visual reveal must be independently scheduled. Concurrency must remain bounded, main-thread work must yield, and hidden tabs must stop scheduling new decode/preparation work.
- The prototype now boots into the persistent Manhattan overview rather than directly into Washington Square. Its temporary determinate loading treatment remains transitional pending the planned avatar/AI conversational startup experience.

## Current codebase

- The manually authored base Union Square introduction is imported through the shared dialogue index and streams after the `union` camera reports that it has settled. It runs when Union Square is chosen from the Manhattan prompt or reached during free exploration, and a session ref prevents it from replaying on later returns.

- Dialogue Markdown now supports inline HTTP/HTTPS hyperlinks using `[label](https://example.com/)` syntax. Caption splitting protects complete link tokens, typewriter playback streams only the readable label rather than exposing Markdown syntax, rendered links open safely in a new tab, and the first Courant narration mention of Tech@NYU links to `https://techatnyu.org/` as the authoring example.

- Dialogue authoring now mirrors the geographic view hierarchy: `manhattan/` contains only the Manhattan overview; the Washington Square overview plus Lipton, Bobst, Stern, and Courant scripts live under `washington-square/`; and `union-square/` contains authored scripts for the overview, 25 Union Square West, and 235 Park Avenue South. Both Union Square landmark scripts are wired to their clickable camera views and use the shared capped streaming plus free-mode Next/Back behavior. The 235 Park Avenue South narration covers U.S. growth, acquisition, launch coordination and reach, media coverage, and leadership on the Indeed Flex partnership initiative.

- Minimized dialogue placement now follows the avatar's snapped corner. Left-docked avatars place the dialogue on their right; right-docked avatars retain dialogue on their left. Either lower corner bottom-aligns the dialogue card with the avatar tile instead of leaving it top-aligned above the bottom edge. Placement updates during dragging and is retained after snapping, with matching desktop and narrow-screen gaps.
- The minimized avatar's default right-corner anchor now uses the docked modal's actual right edge rather than a hard-coded left calculation based on an assumed tile width, keeping its inset stable across desktop monitor sizes while preserving the 12 px narrow-screen inset.
- The minimized avatar render is now centered inside the actual docked modal with a neutral 50% horizontal transform; the prior 59% offset was removed.
- Expanded avatar calls now use a narrow-screen vertical composition below 760 px: dialogue sits above the avatar, while the avatar is centered, reduced, and protected from the desktop horizontal overscan crop.

- Camera dialogue is authored as one Markdown file per scripted view under `src/components/dialogue/`. The renderer segments each imported script around 120 characters, while tour Next and Back controls also navigate between authored stops after overflow is exhausted.

- Avatar one-shots share `avatar-emote-player.ts`. The production `idle.glb` remains the only mounted avatar scene and owns the mesh, skin, materials, textures, face, and idle clip. The wave, nod, and head-shake runtime GLBs are now animation-only: wave is 12 KiB with 19 moving-arm rotation channels; nod and shake are 41 KiB each with only `Head` and `neck` rotation channels. They contain no duplicate mesh, skin, material, texture, or camera, reducing the emote set from roughly 15 MiB to about 94 KiB. The player reconstructs all locked channels from the already-loaded idle clip, preserves exact idle bookends, and remains completion-aware. Versioned animation URLs receive one-year immutable HTTP caching. The unused wave v1 remains removed.

- Docked dialogue now fades in place when Next advances or finishes: its position is retained while invisible, the new blank caption commits before visibility returns, and the docked reveal waits one animation frame so prior long text cannot flash. Past transcript messages remain hidden throughout the entire docked lifecycle instead of depending on the visible-state class, preventing conversation history from flashing during the fade. Camera-dialogue streaming immediately clears stale Yes/No or other prior-phase controls. Escape marks the caption dismissed before minimizing, so it does not reappear beside the docked avatar.

- An isolated review page remains at `/avatar-emote-preview`. It applies its own ten-second automatic demonstration plus a preview-only flat illustrated material treatment: no normal maps, roughness `1`, metalness `0`, texture emission intensity `1.15`, preview exposure `1.25`, greatly reduced geometry-following lights, no camera point light, and a broad low-contrast screen gradient. Production retains the existing toon rendering, while the reviewed nod/smile and disappointed head-shake motion/expression pairs are now also available through the shared production emote player.

- The user's fourth live Blender Texture Paint pass is deployed in `public/animations/idle.glb` as the revised embedded `Mesh_0_BaseColor`. A texture-only GLB replacement confirms that the asset still has 55 nodes, two meshes, one 52-joint skin, three embedded textures, and the 156-channel `Idle_Loop`; only the base-color JPEG payload changed, from 1,404,635 to 1,403,951 bytes. The runtime idle URL now uses the cache key `20260824-user-texture-paint-pass-4` so existing browsers fetch the revision.

- The expanded avatar canvas now extends only its right clipping edge to a fixed `0.375rem` inset, visually matching the near-edge bottom crop and preventing the raised hand from being cut off. Its previous left edge and `translateY(-62%)` vertical framing remain exact; no avatar mesh, Three.js camera constant, scale, or animation code changed. Docked presentation is unaffected.

- A first unapproved voxel facial-animation atlas now exists at `public/style-references/avatar/yuyang-avatar-face-voxel-talking-atlas-v1.png`, with its editable SVG and labeled eight-state contact sheet beside it. It preserves the existing four-by-two neutral/blink/talking state contract while replacing the illustrated curves with crisp flat blocks based on the user's voxel reference. The live Blender connection was unavailable during authoring, so the candidate has not been fitted to the curved face canvas, saved into the authoritative `.blend`, exported to GLB, or wired into runtime; the approved face remains unchanged pending visual acceptance.

- The complete editable avatar working set was preserved before the current repository commit under ignored `data/raw/avatar-working-snapshot-2026-08-24_09-19-39/`. It contains the authoritative 54-bone avatar source and Blender backup, the wave derivative and backup, and all four voxel review rigs. `SHA256SUMS` verifies every preserved Blender file; the committed active sources under `assets/blender/` remain unchanged.

- The unapproved parameterized voxel-avatar generator lives at `scripts/avatar/generate-parameterized-voxel-avatar.py`. After the user rejected V1's flat rectangular head and robot-like arms, V2–V4 shifted facial art direction to the supplied illustrated avatar while retaining the portrait's clothing/palette. Current review V4 has a dense rounded volumetric head, visible large brown eyes and smile, wider shoulders, vertically continuous arms without sleeve clipping, no watch, and separated palm/finger blocks. All visible pieces remain one rigidly weighted mesh on an 18-bone humanoid armature. V1–V3 are rejected provenance; V4 remains unapproved, is not wired into the site, and does not replace the authoritative 54-bone avatar or `public/animations/idle.glb`.

- Agent captions are now gated behind the full 460 ms avatar-shell transition in both directions. Minimizing hides the bubble during the shrink and reveals it only after the dock settles; expanding delays the fullscreen caption fade until the modal reaches its final geometry. A dock-settled ref ensures later streamed dialogue can still reveal immediately when already minimized, while streams begun mid-transition wait for settlement.

- The expansion gate now uses an explicit `expandedSettled` state rather than CSS delay alone. Opening clears docked caption opacity, keeps the chat hidden through all 460 ms of modal resizing, then adds `avatar-call--expanded-settled` and begins a 220 ms fade. This fixes the docked bubble's prior opacity carrying visibly through expansion.

- Scroll-driven arrival at the settled Washington Square camera now triggers the existing NYU/Washington dialogue even when the visitor entered through map interaction rather than the scripted destination button. The trigger is allowed from the pending Washington phase, the Manhattan choice phase, or free exploration, and a session ref guarantees it runs only once without interrupting a declined or different active script.

- The Three.js avatar presentation shifts the fullscreen canvas upward from `translateY(-60%)` to `-62%`, restoring a slightly higher composition without touching the approved GLB or minimized crop. The user restored the first 2D anime-style rendering pass: body, hair, and clothing MeshStandard materials become texture-preserving four-step MeshToon materials at runtime, while the embedded animated face keeps its dedicated MeshStandard/canvas material path.

- The later unlit-material, screen-space outline, face-canvas depth override, and painted chin-curve experiments were rejected and fully removed. The renderer again uses direct scene rendering with no EffectComposer or outline passes, and the runtime face canvas contains only the approved face states and talking-mouth compositor.

- The first avatar appearance now plays `public/animations/wave-hello-review-v2.glb` once before any greeting text begins. `AvatarFullscreen` starts the one-shot only after the avatar's shaders compile and its first frame renders. The call shell does not mount `AgentChat` until the wave mixer emits completion, so the scripted introduction cannot overlap the wave. Runtime normalization plays the source at 1.3× speed, fixes the viewer-left/avatar-right arm to the exact `idle.glb` opening pose, retains rotation only on the viewer-right/avatar-left arm and complete hand/finger chain, locks every translation and scale plus the hips, torso, neck, and head, and overwrites the moving chain's first/last samples with idle values. The wave stays at idle height and hands off without a pose snap; the existing face texture/blink system stays on the same mounted avatar scene.

- Quaternius Universal Animation Library 2 Standard remains preserved as `public/models/quaternius-vrm-animation-library-2.glb`. The attempted `Yes` thumbs-up retarget was abandoned after repeated hand and sleeve review failures. At the user's direction, its clean open-hand recovery was retained instead as `public/animations/wave-hello-review-v2.glb`, with the Action renamed to `wave_hello`; the misleading `thumbs-up-review.glb` path was removed. The retained wave keeps restrained upper-arm participation, an elbow-carried raised open hand, the approved embedded Head-skinned face, planted lower body, and exact neutral bookends. Its editable temporary source is `/tmp/yuyang-wave-hello-review.blend`. Neither wave candidate is wired to the semantic tool pending selection.

- The scripted Washington Square tour now separates its NYU overview from the Lipton Hall residence note. The Washington caption ends with a blue Next action; selecting it starts the authored Lipton Hall camera transition, and the residence caption does not stream until that landmark view reports settled. The Lipton caption also retains a blue Next action for continuing the tour. Scripted and agent-requested pills render inside the latest message card only while minimized; fullscreen mode places them in the normal control area beneath the card, while Reply remains inline.

- Fullscreen blue Next controls now use the same minimum width, padding, and typography as their minimized in-caption counterparts, so switching call modes does not resize the action.

- Docked message bubbles no longer disappear from outside clicks or maximizing the call. Their visibility survives expansion and later re-minimization; the explicit top-left × dismisses directly, while every minimized action pill closes the current bubble before advancing. This supersedes the earlier outside-click and expansion-dismiss behavior recorded below.

- The docked bubble's top-left × now exactly matches the fullscreen close control's 1.25-rem dimensions, red fill, dark-red glyph, type weight, hover color, and keyboard focus ring.

- Every minimized action pill—Yes/No, Okay, neighborhood destinations, guided or agent Next, and Done—now fades and hides the current message over 180 ms before running. Reply is intentionally excluded because it edits the current bubble. Queued continuations explicitly reveal their new bubble after the fade; streamed continuations use the existing automatic reveal.

- An unapproved `wave_hello` candidate exists at `public/animations/wave-hello-review-v1.glb`, authored in `assets/blender/yuyang-avatar-wave-hello-review.blend` directly from the approved 52-bone `Idle_Loop` runtime checkpoint. Weight inspection showed that the inner underarm is dominated by `RightArm` influence and the low-poly shirt lacks enough fold topology for a natural near-horizontal arm raise. A localized reweighting test traded the long torso sheet for a pointed pinch and was rejected/restored. V6 therefore retains the original connected mesh and compatible skeleton, turns the palm outward, keeps the elbow below the shoulder, and lets the bent forearm carry the wave. The four-second one-shot retains the idle lower body, embeds the approved face, exports one finite `wave_hello` animation, and returns to the exact complete starting pose at frame 120 (`0.0` maximum pose-matrix delta). It still requires user approval before runtime integration or promotion.

- Agent answers use the shared 120-character segmentation and manual overflow queue. Final Done minimizes the guide after the last segment while Reply remains available.

- The initial greeting no longer hardcodes Yuyang's age. It calculates completed years from October 29, 2006 against the browser's current local calendar, so the displayed age advances automatically on the birthday. Separately, every accepted agent request generates a fresh ISO UTC timestamp for that exact turn and places it at the top of the model's developer context before the current camera and retrieval evidence.

- Any scripted or server-driven text stream now automatically reveals the newest caption immediately to the left of the minimized avatar. The card is aligned by its right edge against the avatar, remains present after streaming completes, and disappears only through its small ×, an outside click, or expanding the call; expanding clears it so re-minimizing does not resurrect the dismissed caption. Completed history, Reply, choice, and continuation controls remain hidden beside the dock. The expanded caption layout is unchanged.

- The agent and live map now read from one camera-view registry at `src/agent/camera-views.ts`. It currently exposes four geographic views (`globe`, `manhattan`, `washington`, `union`) and six authored landmark views (Bobst Library, Lipton Hall, Courant Institute, Stern School of Business, 25 Union Square West, and 235 Park Avenue South). The OpenAI tool enum, validation, prompt-visible view list, interactive landmark IDs, camera framing, cross-neighborhood routing, deferred post-load selection, and reported current view all derive from this registry, so adding or editing a record updates agent availability without maintaining a second list. Runtime registration fails visibly if an interactive landmark lacks a camera record. Multi-level agent navigation fully settles every intermediate authored camera before advancing—for example, a Bobst request from the globe completes Globe → Manhattan → Washington Square → Bobst instead of overwriting the Washington destination mid-flight.
- The inline reply composer's circular send arrow now uses the same layered blue fill as the affirmative `Yes` tour choice instead of the prior muted teal; its geometry and disabled opacity remain unchanged.

- A landmark-selected close view can now transfer directly to another interactive building: inward scrolling over the other building raycasts it while the camera remains locked and routes through the same `selectLandmark` path, so the destination uses its grounded bounds and existing authored camera preset without first returning to the neighborhood overview. The existing outward-scroll exit remains unchanged.

- `public/animations/idle.glb` now literally contains the neutral illustrated face as its second skinned mesh. After the automated centering attempts were rejected, the user manually positioned and rotated the face in the live Blender viewport and explicitly approved that result. The authoritative transform is location `(-0.000442441, -0.065205455, 0.336710483)`, XYZ Euler rotation `(0.280676663, -0.085589372, 0.202968687)`, and scale `(1.164230943, 1.164231062, 1.164230943)`. Location, rotation, and scale are fully locked, and all 442 vertices remain weighted exactly 100% to `Head`. The exported GLB retains 52 bones, one `Idle_Loop` spanning exactly 150 frames at 30 fps, and `0.0` full pose-matrix loop closure. Full-screen framing remains at 225% of the original scale with a vertical target of 83% of avatar height and a scale-compensated horizontal offset.
- The browser now animates face states on that same embedded Head-skinned mesh. A 512² runtime canvas independently selects the exact approved open/closed-eye artwork and composites one of four transparent mouth-only derivatives from the approved atlas. Idle keeps two 120 ms blinks during each five-second loop; streamed speech opens immediately on a wide state and advances at an 80 ms cadence with only one brief neutral-mouth beat per eight states. The blink clock continues independently during speech. Ending speech restores neutral immediately; no second GLB, body retarget, detached eyelid, or renderer remount is involved.

- `public/animations/idle.glb` is now the approved live idle. The user's frame-4 manual arm/forearm pose offsets were baked across the complete clip, both sides remain symmetric beside the torso, the lower body stays planted with restrained foot spacing, and the two hand bones plus all 30 finger joints are held in their fitted open neutral orientations so the hands hang parallel to the torso. The former review filename was removed after promotion. Blender 5.1 validation retains 52 bones, the `Head` attachment required by the runtime face canvas, one `Idle_Loop` over frames 0–60, `2.39e-7` maximum neutral hand/finger matrix drift, and exact `0.0` full pose-matrix loop closure. The idle loader cache key is `20260821-approved-open-hands-planted-idle`, so the FaceTime avatar now uses this animation with the existing illustrated face attachment.

- The later shoulder-joints-only candidate was also rejected because moving the downstream rest-bone chains damaged the finger deformation. `public/animations/idle-shoulder-joints-review.glb` was deleted, all experiment objects and orphaned Actions were removed, and exactly one pristine `Idle_Loop` import from `public/animations/idle.glb` remains visible in Blender with the rig overlay hidden. The accepted idle still matches its pre-experiment SHA-256 `d78abbf1e221741737df06b596c8bec02a04310b547d80b3ab1f15cf42e74a39`. No shoulder experiment remains active.

- The first functional guided-agent slice lives under `src/agent/` with a server-only streaming `POST /api/agent` boundary. It now performs private Pinecone retrieval only when required by the dynamic memory policy; retrieval failure prevents an ungrounded personal answer. Assistant words stream into a compact Apple FaceTime-caption-inspired dark liquid-glass block with a `Yuyang` speaker line. Tool-round draft text is buffered and discarded so private retrieval never leaks pre-tool prose; `speech_start` fires only for the visible final answer. Later non-whitespace deltas maintain a 58 ms throttled cadence. The CC0 blip plays at 1.4× with pitch preservation off and volume `0.11`.
- Production first load begins with a solid-white full-screen loader containing an animated inline globe SVG. Its “Building your world…” phase lasts exactly two seconds, then exposes “Press to continue.” That gesture primes the shared dialogue audio and removes the loader; non-production builds bypass it. The agent UI additionally waits for avatar first-frame readiness and a 500 ms visual beat before mounting. It then queues `wave_hello`, progressively streams the birthday-derived-age introduction, and fades in dark liquid-glass `Yes`/`No` controls. `Yes` streams “Gotcha, let me show you around.”, minimizes the guide, and starts the authored globe-to-Manhattan zoom/cloud flight. Once the Manhattan camera fully settles, the minimized guide streams: “Welcome to Manhattan! I'm currently a Junior at NYU, and I spend most of my time split between either Union Square or Washington Square. Where would you like to go first?” The arrival is driven by a reusable settled-view event and fires only once in the intro tour state. After the stream completes, two white liquid-glass `Union Square` and `Washington Square` buttons appear inside the latest caption—even while docked—and each minimizes the guide before routing through the existing authored camera navigation. Their pointer and click events are isolated from the docked avatar shell, so selecting either while minimized cannot reopen fullscreen. Choosing Washington Square queues a second arrival-gated caption after the neighborhood camera settles: “Washington square is where NYU has it's campus. This is where I take my classes, go to club meetings, and study. I also lived at Lipton hall, on the west side of the park my Freshman year!” `No` streams “Gotcha, feel free to take a look around.” and fades in an `Okay` button that minimizes the persistent call. The guide state remains mounted so reopening does not replay the first-load wave and choice. The newest caption keeps a Reply action below its text except while the destination buttons are active; selecting Reply otherwise replaces that row in place with the normally hidden inline composer inside the same glass card. Submitting creates the next assistant card immediately with animated dots, which the first streamed character replaces.
- Guided-tour narration uses the same 120-character segmentation. Blue Next reveals queued overflow before navigating to the next authored stop; gray Back retains its tour-history behavior.
- Standalone Reply spacing is tightened to a `0.25rem` top margin and `0.2rem` top padding so the space above and below it is visually balanced. Opening Reply now shortens the flexible text field to accommodate both the circular blue send arrow and a plain blue `Cancel` text action at `0.66rem`/500 weight; Cancel closes the composer and restores the prior caption actions without expanding the avatar.
- Agent camera tools may not cut between view modes. The runtime routes every `navigate_map` command through the authored camera animation path: globe transitions use the cloud flight, neighborhood/Manhattan transitions use camera interpolation, and multi-level requests queue Manhattan as an animated intermediate stage. Assistant captions accumulate in an assistant-only FaceTime history with older entries visually subdued, a larger vertical gap, a 30%-wider maximum caption column, a five-rem left inset, and matching two-rem top/bottom insets. Live-offset scrolling has explicit endpoints: maximum upward scroll reveals the complete history; the default and maximum downward state show the latest caption plus half of the preceding caption, extending farther only when necessary to keep active controls visible. New messages enter at that default/downward endpoint. This avoids ResizeObservers and stale stored-coordinate rewrites. Each newly inserted assistant card uses a 360 ms opacity/lift/scale entrance with a reduced-motion fallback. Caption cards/controls cannot flex-shrink when invisible scroll room is present, preserving full text height. Caption cards are paint-contained to prevent filtered compositing rectangles from leaking across the viewport, and repeated scripted captions are deduplicated. The caption glass is 20% lighter without a perimeter stroke; `Yes` uses blue liquid glass and `No` uses white liquid glass. Reply may open its inline composer without removing the active scripted choice pills. The complete caption/control layer fades immediately on minimize and fades back only after the 460 ms modal expansion reaches full size.
- The OpenAI tool loop exposes validated navigation, avatar-emote, and presentation controls. Browser commands cross a typed event boundary; the map runtime executes navigation while animation filenames/timing remain client-owned. The endpoint bounds message size/history, treats RAG chunks as untrusted evidence, and applies a basic per-instance request limit. Retrieval source metadata remains available in the stream contract but citation chips are intentionally hidden from the FaceTime interface.

- The shoulder-extension experiment was rejected and deleted after animated shoulder translations catastrophically tore the weighted sleeve/chest surface and destabilized the hands. All experimental objects and orphaned review Actions were removed from the live Blender scene, and the untouched `public/animations/idle.glb` was reimported and framed for review. Its SHA-256 remains `d78abbf1e221741737df06b596c8bec02a04310b547d80b3ab1f15cf42e74a39`. Any future shoulder-width correction must begin from the original and solve joint placement, rest matrices, mesh deformation, and weights together; do not translate weighted shoulder pose bones as a shortcut.

- The protected torso-corrected idle mapping checkpoint now lives at `public/animations/idle.glb`, and the live avatar loader reads that URL directly. `public/animations/` is the permanent destination for all future standalone exported animation GLBs; `public/models/` no longer owns individual animation outputs.

- `public/models/` was reduced to active map assets, the two authoritative 54-bone V2 avatar exports, the live embedded-idle avatar, the Quaternius library/reference pair, and the explicitly protected torso-corrected mapping checkpoint. Superseded mesh experiments, comparison rigs, recovery exports, rejected retarget diagnostics, and the unused watercolor globe web export were deleted from the public runtime directory. Older milestone bullets that name those deleted review binaries describe historical work only and do not make them active assets.

- The former 3,507-line map component now lives entirely under `src/components/map/`. `map.tsx` is a three-line public entry, `map-shell.tsx` is its browser-only loading boundary, and `runtime/map-runtime.tsx` owns persistent React/Three.js orchestration. Globe construction lives under `globe/`; Manhattan navigation, waterfront, ambient systems, and nested neighborhood/landmark code live under `manhattan/`; shared renderer creation, frame composition, types, constants, and logging live under `shared/`.
- The runtime frame loop delegates its independent globe-cloud, landmark, traveler, marker, spring-arrival, adaptive-resolution, and rendering systems to focused modules under `animation/` and `shared/rendering.ts`.
- ESLint now enforces a 900-line maximum for authored JavaScript and TypeScript files while excluding deterministic `src/generated/` outputs. The current refactor leaves all authored source within that cap when blank and comment lines are excluded.
- The complete pointer/touch/wheel/keyboard input state machine is isolated in `map/interaction/input-controller.ts`, including pointer capture, mouse/touch dragging, pinch routing, gesture debounce timers, hover throttling, landmark selection, and geographic view clicks. The runtime orchestrator is now 866 physical lines.
- Map hit-testing is isolated in `map/interaction/hit-testing.ts`, covering ray setup and globe, landmark, navigation-arrow, Manhattan-marker, and park destination intersections. Viewport suspension, visibility handling, animation start/stop, and observer teardown are isolated in `map/runtime/viewport-lifecycle.ts`. The runtime orchestrator is now 1,029 physical lines.
- Manhattan staged loading is isolated in `map/manhattan/loading/manhattan-loader.ts` behind a typed contract. It owns road GLB loading, CityGML tile streaming and cleanup, dataset decoding, park/landmark assembly, routes, ambient populations, and reveal sequencing. Shared runtime resource disposal is isolated in `map/runtime/dispose-runtime.ts`; the orchestrator is now 1,090 physical lines, down from 1,428 before this pass.
- Concise `info.md` ownership guides now document every meaningful repository directory, including authoritative-source, regeneration, milestone, landmark-integrity, avatar-rig, and Next.js workflow boundaries. Dependency, cache, generated-output, and temporary work directories are intentionally excluded.
- Repository policy now requires a clear English `INFO.md` in every folder. Each file documents that folder's purpose, every direct subfolder, and every direct file, and must be updated in the same change whenever contained files or direct subfolders change.
- The site now uses the user-supplied illustrated Yuyang portrait as its App Router favicon at `src/app/icon.png`, exported as a 512×512 RGBA PNG with the off-white backdrop removed to full transparency. The browser title is `Welcome to Yuyang's World!`.
- The first Meshy variant remains the body, hair, wardrobe, and proportion source. Meshy's stock Idle and all attempted topology-driven talking animation are rejected. True lip-sync, TTS, per-response video generation, and frame-exact response-duration matching are not required; the target remains a small, intentional game-dialogue animation set.
- A local `MESHY_API_KEY` is configured for authenticated Meshy API work. Its value is secret and must never be printed, logged, committed, or copied into project documentation; any Meshy automation must read it from the environment at runtime.
- Meshy T2 Smart Topology regeneration task `01a0256f-a640-74f0-95af-67e643ca4840` produced the accepted-for-testing blank-face candidate at a 15,000-face target, A-pose, and 4K base color without generated PBR maps. The raw result has 11,374 vertices and 15,341 polygons and still contains a subtle generated nose despite the featureless source; the user accepted it for a rigging trial. A temporary Blender review under `/tmp/yuyang-meshy-smart-topology-review-01a0256f/` transfers the authoritative V2 skeleton and talking Action. Automated checks confirm 54 bones, all 30 finger bones, 54 vertex groups, complete weight coverage across all 11,374 vertices, finite deformation at five animation samples, and exact frame 1/89 loop closure (`0.0` maximum bone-matrix delta). The candidate and transferred hand deformation remain visually unapproved and must not replace the authoritative Blender source yet.
- The accepted-for-testing Meshy T2 surface is now deployed to both `public/models/yuyang-avatar-idle-loop-v2.glb` and `yuyang-avatar-talking-loop-v2.glb` for live site review. Each web GLB preserves its prior native idle or talking Action binding, the 54-bone skeleton, all 30 finger bones, 54 deform groups, the `Head` attachment used by the runtime face canvas, and the new 4096² base-color texture. Clean Blender round-trips validate one intended Action per file and the full 11,374-vertex/15,341-polygon surface. The initial deployment used cache key `20260821-meshy-t2-rig-transfer-review`; automated browser control was unavailable, so appearance and hand deformation still require user review before the Blender authoring source is replaced.
- User review found the first Meshy T2 site pass too faceted and the attached 2D face oversized/high, while the first reduced face correction was too small. The current site-review GLBs clear Meshy's imported custom normal field and mark all 15,341 polygons smooth without moving geometry; they retain the validated 54-bone/30-finger structure and use cache key `20260821-meshy-t2-soft-normals-face-fit`. Runtime roughness is normalized from the out-of-range `2.5` to `1.0`. The face canvas is now a middle-size `0.19 × 0.236` world units at Y `1.405` and centered on X `0`, aligning the illustrated nose to the replacement mesh's generated nose. Targeted ESLint and HTTP checks pass; this revised surface/face fit awaits visual approval.
- Follow-up review showed that clearing custom normals alone did not remove the facial facets. Analysis found 3,389 removable exact-position duplicates across 2,839 split locations in the Meshy mesh; only seven duplicate buckets had conflicting bone-weight signatures. The current `20260821-meshy-t2-safe-weld` review source welds the 3,382 duplicates whose weights match, leaves all seven rig-sensitive conflicts untouched, preserves the UV layer and all 15,341 polygons, and reduces the Blender source to 7,992 vertices without changing sampled animation bounds or `0.0` loop closure. glTF necessarily re-splits the surface to 11,374 vertices at UV/attribute boundaries but now carries averaged normals across those splits. Both idle and talking GLBs pass clean 54-bone/30-finger round-trips; targeted ESLint and HTTP checks pass. Visual approval is still pending.
- The old facial atlas fit was abandoned for the replacement head. Image generation produced a new cohesive eight-state transparent face set tailored to the Meshy T2 proportions; the untouched 1254² generation source is `yuyang-avatar-face-talking-atlas-meshy-t2-v1-source.png`, and a deterministic layout normalization creates the runtime-ready 2048×1024 `yuyang-avatar-face-talking-atlas-meshy-t2-v1.png` with eight 512² tiles ordered neutral, half blink, closed blink, small talk, medium talk, wide talk, rounded talk, and smiling talk. The live idle renderer now samples the new atlas's first tile. After review found the initial regenerated face too large and high, the current curved canvas is `0.15 × 0.186` world units at X `0`, Y `1.365`, retaining the new head's centerline while moving the full face down. The generated atlas and fit remain review candidates; targeted ESLint, whitespace, and atlas/page HTTP checks pass.
- The nosed T2, safe-weld, and replacement-face-atlas live passes above are superseded. Strict blank-face multiview source task `01a0258f-a725-7374-94f1-731a8718b486` led to Meshy 7 task `01a02596-de15-7f83-94a6-598e3d154207`, which passed front and side inspection with a flat facial plane. The accepted geometry was remeshed first at a 30,000-face target (`01a0259a-3c3b-715f-92e7-36ef6bdb6c15`) and only then retextured at 4K without PBR maps (`01a0259d-97bd-725a-8217-f026137b43fe`), avoiding the reprojection order behind the prior seams.
- The current public idle and talking GLBs place that surface on the complete V2 hierarchy: 54 bones, all 30 three-joint finger bones, 54 deform groups, 34,098 vertices, 31,319 polygons, the `Head` attachment, and exactly one preserved animation per file. User review found the 40° upper-arm correction converged too far across the torso, so the live pass now uses a midpoint 29° mirrored correction while retaining downstream forearm, wrist, hand, and finger motion. Hair selection traverses coincident split-vertex coordinates as well as shared indices, covering 16,402 dark/tan-contaminated faces. The live shell uses the old approved `yuyang-avatar-face-talking-atlas.png` at its original scale and center `(-0.004, 1.4145, 0.166)`, exactly 10% of the canvas height below Y `1.44`, behind cache key `20260821-arm-balance-v4`. The authoritative Blender source remains unchanged until visual approval.
- A non-production restart repeats the topology-first Meshy order from the same strict source: native geometry `01a02610-c082-720a-b45e-b1b344a066f5`, 30k remesh `01a02615-9c31-731c-b14f-eb686a9fe481`, 4K non-PBR retexture `01a02616-f442-7036-a0f0-049c83138931`, and auto-rig `01a02619-8d13-7573-9ffe-bdfc42bb8b82`. The auto-rig has the exact same 24 body-bone names as the authoritative Meshy hierarchy. A Blender-only review applies `Yuyang_Talking_Loop_v2`, verifies non-static deformation and matching frame-1/frame-89 bounds, and intentionally omits the 30 finger joints. It is not deployed and must pass visual review first.
- The separate auto-rig review now has a localized manual nose-flattening candidate. An elliptical falloff moves 1,013 central facial vertices by at most `0.064821` meters while preserving the UVs, 24 deform groups, 24-bone hierarchy, and talking Action. The round-trip GLB retains 30,951 polygons and one animation. The faint front-view nose mark is baked base color, not remaining projection. `yuyang-avatar-meshy-auto-rig-face-flattened-review.blend` is open for inspection; neither it nor its GLB is deployed or authoritative.
- The approved replacement facial direction is a cohesive 2D illustrated face on a dimensional 3D avatar: eyes, eyelids, pupils, eyebrows, nose marks, mouth, cheeks, and facial shading must be designed as one unified 2D system, while the cranium, ears, neck, hair, body, clothes, hands, and shoes remain 3D. This supersedes both the failed sculpted-mouth work and the rejected experiment that pasted isolated 2D ovals onto the old 3D facial features.
- `public/style-references/avatar/yuyang-avatar-2d-face-approved-concept.png` is the approved visual-direction image. Meshy then generated `public/style-references/avatar/yuyang-avatar-meshy-blank-face-source.png`, a faithful full-body A-pose source whose head is natively featureless: no eyes, sockets, eyelids, eyebrows, nose, nostrils, mouth, lips, face panel, or seam.
- Meshy Image-to-3D task `01a02230-0821-702b-a9fa-ae63a04ab9a5` generated the real replacement model using T2 Smart Topology, a 15,000-face target, 4K texturing, and A-pose output. The production artifacts are `public/models/yuyang-avatar-meshy-blank-face.glb` and `assets/blender/yuyang-avatar-meshy-blank-face.blend`; the imported main mesh has 12,602 vertices and 15,702 faces. Four Meshy review thumbnails are stored beside the source image. The rejected hand-authored facial-canvas attempt was removed from active assets and moved under ignored `data/work/rejected-avatar-canvas/` for recovery only.
- Meshy's blank face initially showed rough, faceted lighting. A first vertex-displacement smoothing attempt exposed unwelded seams as visible cracks and was fully reverted from an internal recovery copy. The accepted correction changes only facial polygon shading/normals across 749 front-head polygons; it moves no vertices, preserves the continuous original geometry, and removes the rough lighting without cracks. The corrected Blender file and GLB overwrite the earlier unsmoothed production artifacts. `public/style-references/avatar/yuyang-avatar-meshy-blank-face-smoothed-front-v2.png` is the accepted technical preview.
- Because the T2 topology remained visibly uneven, Meshy regenerated the same clean blank-face source as task `01a0223b-fba2-74f2-bb1b-63735ff4a297` using Meshy 7 Standard with Ultra mode, native high-detail geometry, no remesh, image enhancement disabled, 4K texture, and A-pose output. This 35-credit pass removes the fractured center ridge and is stored non-destructively as the 118 MiB `public/models/yuyang-avatar-meshy-blank-face-v2.glb` with four `v2` review thumbnails.
- The approved v2 shape is optimized for web delivery in `assets/blender/yuyang-avatar-meshy-blank-face-v2-optimized.blend` and `public/models/yuyang-avatar-meshy-blank-face-v2-optimized.glb`. Blender reduced the untouched 3,115,106-face/4K source to 39,999 faces with 2K base color, preserving the smooth blank face, hair silhouette, UV layer, and material. The GLB is 6,409,008 bytes, a 94.8% reduction from the 124,209,920-byte source, and passes a clean import round-trip. Full-body and face close-up review renders are stored as `yuyang-avatar-meshy-blank-face-v2-optimized-preview.png` and `yuyang-avatar-meshy-blank-face-v2-optimized-face.png`.
- Talking-face runtime direction is now a deterministic texture-atlas animation on the regenerated head's existing facial UV region, not a GIF/video and not a separate face mesh. One shader/material samples the unchanged base texture plus a true-alpha atlas containing complete, consistently aligned face states; a frame uniform selects neutral, half blink, closed blink, small/medium/wide/rounded talk, and smiling-talk states. Blink and mouth schedules may advance independently in application state, but each displayed atlas tile is a cohesive full-face drawing so features never detach. `public/style-references/avatar/yuyang-avatar-face-talking-atlas-draft.png` is layout exploration only: image generation produced a baked checkerboard and therefore it is explicitly not a production alpha texture.
- The first deterministic atlas is authored as editable `public/style-references/avatar/yuyang-avatar-face-talking-atlas.svg` and true-alpha `yuyang-avatar-face-talking-atlas.png`. Its eight exactly aligned tiles cover neutral, half blink, closed blink, small/medium/wide/rounded talk, and smiling talk. A five-second 24 fps timing study composites those states over the optimized blank-face render with two blinks placed during closed-mouth pauses and restrained four-frame mouth holds. Review deliverables are `yuyang-avatar-talking-face-2d-v1.mp4`, a 12 fps GIF, and a six-frame contact sheet. This v1 is an image-space look/timing proof only; production still requires sampling the same atlas in the live head material.
- User review approved the overall talking treatment but rejected the two blush/cheek ellipses. They were removed from the editable atlas definition and every PNG/video/GIF/contact-sheet derivative was regenerated in place; current v1 files contain no cheek marks.
- Meshy rigging task `01a02257-9730-76fa-8410-ce0a57bcfa08` successfully rigged the optimized v2 for five credits. The first arms-down rig export was invalid: glTF reopened in the original A-pose, and a temporary website experiment compounded the error by positioning the face atlas as an independently scaling DOM layer. User review rejected that result. All temporary live-model component/CSS integration was immediately removed, restoring the prior approved static placeholder.
- A corrected static web model is now baked at `public/models/yuyang-avatar-arms-down-static.glb` with authoring source `assets/blender/yuyang-avatar-arms-down-static.blend`. The upper arms retain the relaxed lowered pose and share a 30° counterclockwise swing around the body's vertical axis, measured from the neutral relaxed pose. The evaluated armature deformation is baked into a scale-normalized 1.7-meter mesh with 39,604 faces and no armature modifier. The three-quarter review is `public/style-references/avatar/yuyang-avatar-arm-swing-review.png`. It is not wired into the website yet.
- The first production body-animation candidate is a restrained four-second natural idle authored on the still-rigged arms-down blank-face v2 base. `assets/blender/yuyang-avatar-idle-v1.blend` retains the 24-bone humanoid skeleton and editable action `Yuyang_Idle_Natural_Loop_v1`; `public/models/yuyang-avatar-idle-v1.glb` is the web export, and `public/style-references/avatar/yuyang-avatar-idle-v1.mp4` is the 540×720 review loop. After the first review exposed a lingering A-shaped arm gap, both upper arms were lowered another 22° in opposite local directions; they now hang beside the torso with the hands alongside the thighs, and their secondary motion is limited to roughly a tenth of a degree. Frames 1 and 121 are numerically identical across every pose-bone matrix (`0.0` maximum delta), while the MP4 contains frames 1–120 so the duplicate boundary pose is not displayed twice. Remaining motion is limited to slow breathing, slight torso/hip sway, shoulder settling, and restrained neck/head counter-motion. The facial surface remains blank for the later UV-linked 2D atlas. This v1 is awaiting user visual approval and is not connected to the website.
- Quaternius's downloaded CC0 Universal Animation Library Standard GLB was inspected and its `Idle_Loop` and `Idle_Talking_Loop` actions were retargeted from the 65-bone source skeleton to the avatar's 24-bone Meshy skeleton. The first comparison was invalid because hiding the source armature froze Blender's pose evaluation and produced static clips; the next absolute-rotation pass was rejected because it transferred the library's incompatible rest stance. The corrected retarget keeps the source armature evaluable and transfers each bone's motion delta relative to that clip's first frame onto the avatar's approved neutral rest pose. The combined editable result is `assets/blender/yuyang-avatar-library-idles.blend`; `public/models/yuyang-avatar-library-idles.glb` contains exactly the two clean named Actions `Yuyang_Idle_Quaternius` and `Yuyang_Idle_Talking_Quaternius`. Automated validation rejects static output and confirms exact numerical loop closure (`0.0` maximum first/last pose-bone matrix delta). Corrected 540×720, 30 fps reviews are `public/style-references/avatar/yuyang-avatar-quaternius-idle.mp4` (2.5 seconds) and `yuyang-avatar-quaternius-talking-idle.mp4` (2.933 seconds). After removing hand/wrist channels still allowed forearm motion to carry the rigid hand through the thigh, the entire shoulder-to-hand animation chain was locked: shoulder, upper-arm, forearm, wrist, and hand source motion is omitted, while each upper arm keeps a constant mirrored 22° arms-at-sides base pose. An eight-sample full-cycle review confirms the hands remain outside the thighs without twisting or clipping while torso, hips, legs, neck, and head animate. The incompatible Quaternius source hand channels remain permanently excluded; later custom hand controls must be reviewed independently before any gesture animation. Neither candidate is connected to the website pending user selection.
- The rejected procedural replacement-hand experiment was removed from production and isolated under ignored `data/work/rejected-fixed-hands-2026-08-20/`; its authoring script was deleted. The untouched original generated hand mesh is retained. A new hand-rig candidate adds five deform controls per side—thumb, index, middle, ring, and pinky—directly to that original geometry without changing topology. `assets/blender/yuyang-avatar-hand-rig-v1.blend` is the editable 34-bone source and `public/models/yuyang-avatar-hand-rig-v1.glb` is the web round-trip. Neutral evaluated geometry is preserved within `0.000000273` maximum vertex displacement, and the exported GLB retains all ten new hand bones plus both Quaternius idle Actions. Finger weights are derived from the existing hand surface and remain a candidate pending range-of-motion/deformation review; no hand gesture animation has been authored or approved yet.
- A separate full-motion test re-enables the Quaternius shoulder, upper-arm, forearm, wrist, hand, thumb, and first finger-chain channels on the 34-bone candidate without changing the collision-safe idles. `assets/blender/yuyang-avatar-hand-motion-test.blend` and `public/models/yuyang-avatar-hand-motion-test.glb` contain `Yuyang_Idle_Quaternius_Hands` and `Yuyang_Idle_Talking_Quaternius_Hands`. Review MP4s are `public/style-references/avatar/yuyang-avatar-quaternius-idle-hands-test.mp4` and `yuyang-avatar-quaternius-talking-idle-hands-test.mp4`. Both clips retain exact loop closure and the GLB round-trip retains all 34 bones. The new weights prevent the earlier catastrophic hand split, but the strongest talking gesture still produces a visibly splayed right-hand pose; this branch is comparison-only and does not replace the locked-arm candidate pending user review.
- The first full-hand test was rejected: independent library finger chains visibly clawed the fused original hand surface, and unlocked arm retargeting omitted the mirrored 22° neutral base, causing frame one to fall toward an A-pose. The corrected comparison now keeps all ten custom finger/thumb bones neutral, transfers only shoulder/upper-arm/forearm/wrist/whole-hand motion deltas, and layers those deltas over the approved arms-at-sides base. The corrected MP4s overwrite the same `*-hands-test.mp4` paths. Default pose and loop closure are correct, and the claw deformation is gone. Near the strongest talking gesture, the rigid original thumb/mitten silhouette rotates toward camera and approaches the thigh; this remains a review-only result rather than a production selection.
- Final user review rejected the corrected hand-motion comparison as well: the talking source drove the rigid original hand into the front of the thigh/body. The entire hand-motion test branch, its wrapper script, Blender/GLB outputs, and both MP4s were removed from production and isolated under ignored `data/work/rejected-hand-motion-test-2026-08-20/`. Automatic Quaternius forearm, wrist, hand, and finger motion remains rejected; later V2 clips may use visually reviewed upper-arm motion while keeping those distal joints neutral.
- At the preceding v1 stage, `AGENTS.md` temporarily identified `assets/blender/yuyang-avatar-hand-rig-v1.blend` and its 34-bone web export as authoritative. That record is historical and is superseded by V2 below.
- The v1 hand candidate is superseded. The authoritative V2 avatar has 54 bones: the original 24-body skeleton plus three connected deform joints for each of ten digits. Its neutral evaluated surface differs from the pre-rig mesh by at most `0.000000369` units. The public avatar GLBs are now `public/models/yuyang-avatar-idle-loop-v2.glb` and `public/models/yuyang-avatar-talking-loop-v2.glb`, each containing the same skeleton and exactly one named Action.
- The standard library's `Idle_Loop` was retargeted onto that V2 skeleton as a 2.5-second loop. Automated validation finds non-static body motion and exact first/terminal pose closure (`0.0` matrix delta). The source clip has no wrist or finger keys, so all new hand joints remain neutral; a seven-sample visual review keeps both hands beside and outside the thighs. The 30 fps review MP4 is `public/style-references/avatar/yuyang-avatar-idle-loop-v2.mp4`, excluding the duplicate terminal frame.
- The matching standard-library `Idle_Talking_Loop` is retargeted onto the same V2 skeleton as `Yuyang_Talking_Loop_v2`. Authoring and web outputs are `assets/blender/yuyang-avatar-talking-loop-v2.blend` and `public/models/yuyang-avatar-talking-loop-v2.glb`; the 30 fps review is `public/style-references/avatar/yuyang-avatar-talking-loop-v2.mp4`. After review exposed frozen hands and a right-hand/thigh intersection, the retarget was corrected to map both complete shoulder, upper-arm, forearm, hand, and 30-bone digit hierarchies. Shoulder/upper-arm deltas are strongly damped while wrist and finger deltas remain visible, keeping the hands outside the pants across nine full-cycle samples without splitting or clawing. Measured local-pose deltas confirm hand motion (`0.126699` left, `0.138645` right) and animated right index/thumb chains. It runs 2.933 seconds without displaying its duplicate terminal frame, remains non-static, closes exactly (`0.0` first/terminal pose delta), and clean GLB import retains all 54 bones and exactly one Action.
- The approved cheek-free 2D talking atlas is now combined with the corrected talking body loop in `assets/blender/yuyang-avatar-talking-loop-face-v2.blend`. A curved, `Head`-weighted `Yuyang_BlankFace_Canvas` samples all eight coordinated full-face atlas states; the deterministic 89-frame schedule uses short talking-mouth holds and two neutral-mouth blink arcs, with frames 1 and 89 both neutral. User review found the initial 540x720 three-quarter render soft when enlarged and the face canvas vertically high. The corrected canvas is lowered by `0.035` world units, uses cubic atlas sampling, and is rendered from a straight-on camera at 1080x1440. The updated 30 fps review `public/style-references/avatar/yuyang-avatar-talking-loop-face-v2.mp4` contains frames 1-88 for a 2.933-second seamless playback without a duplicate terminal frame. The canvas follows the animated head in 3D rather than using a DOM/screen-space overlay. Production WebGL still needs to reproduce the atlas-state schedule because glTF does not carry Blender node-socket keyframes as a self-playing material animation.
- A later color review found the face artwork too shallow and requested wider eye spacing. The editable SVG and regenerated transparent PNG now move each eye/brow group 14 atlas pixels outward, increasing pupil-center separation from 164 to 192 pixels consistently across open, half-blink, and closed states. The Blender face material applies 1.45 saturation, 0.72 value, and 0.22 contrast with emission reduced from 0.35 to 0.08, producing deeper brows, pupils, nose, and mouth colors without changing the underlying skin/body material. The combined Blender source and 1080x1440 MP4 were regenerated in place.
- The deeper first pass made the nose read red. Per the user's preview-before-integration request, two isolated candidates were rendered without modifying the active animation. The user approved the cooler neutral-taupe candidate (`#6f6b67` source color), which is now applied consistently to both nose paths in the editable SVG, regenerated true-alpha PNG, combined Blender source, and final 1080x1440 talking-loop MP4. The rejected warm-brown candidate remains only under ignored `data/work/avatar-face-color-candidates/`.
- Active Blender-source cleanup now keeps only `assets/blender/yuyang-avatar-talking-loop-face-v2.blend` for the avatar and `assets/blender/lowpoly-earth-watercolor.blend` for the globe. Twenty-one unused avatar `.blend`/`.blend1` files moved intact to ignored `data/raw/avatar-blender-archive-2026-08-20/`; the watercolor globe backup and obsolete yellow-facet preview pair moved to `data/raw/globe-blender-archive-2026-08-20/`. This is recoverable archival, not deletion. The unrelated active `nyu-custom-buildings.blend` landmark source and its Blender backup remain in place.
- Public cleanup removed all obsolete avatar GLBs and the explicitly retired `public/photos`, `public/textures`, and `public/branding` directories. The identical POI exclamation graphics are now generated directly by the existing canvas fallback, so deleting the branding SVGs does not create broken requests. Scripts are organized by use case under `scripts/avatar/`, `scripts/map-data/`, and `scripts/previews/`; package commands and generated-file provenance comments use the new paths.
- `.codex/skills/blender-avatar-authoring/` is the validated repository-local workflow for future avatar agents. It pairs with the installed `blender-animation-rigging` skill, requires live-Blender inspect/mutate/screenshot review, routes current work to the approved cohesive 2D face canvas, requires labeled range-of-motion galleries before animation renders, and records the rejected flat-overlay/detached-mouth/topology shortcuts. The original `assets/blender/yuyang-avatar.blend` was restored from its untouched `.blend1` backup and saved clean with 31,316 vertices, 31,208 polygons, and only its original `Basis`/`Mouth_Open` keys; the rejected Blender talking v1/v2 videos and obsolete authoring script were moved out of the repository to `/tmp/yuyang-rejected-avatar-2026-08-20/`.
- The first-load FaceTime shell now replaces the static portrait placeholder with one continuously mounted Three.js `yuyang-avatar-idle-loop-v2.glb`. Browser-only rendering is organized under `src/components/avatar-view/`; `avatar-fullscreen.tsx` owns the single authoritative right-offset camera (`center = [-0.38, 1.45]`, base orthographic height `0.93`), shared lighting, approved closed-mouth smile attachment, render loop, and cleanup for every current and future avatar state. The earlier `0.86` height was widened by about 8% to give the fullscreen avatar more breathing room. Its canvas and orthographic span both receive 30% vertical overscan, preserving avatar scale while rendering beyond the modal's lower edge to prevent the previous straight torso cutoff and residual bottom strip. `idle/idle.tsx` owns only the idle GLB load and fixed-body mixer. Minimizing never unloads or reframes the Three.js scene: the shell linearly translates and scales the unchanged full-resolution canvas over 460 ms. The expanded canvas sits at `translateY(-60%)`; the manually tuned minimized crop remains `translate(-58.5%, -49%) scale(0.34)`. The vertically centered shell retains its original responsive 76rem width, 48rem maximum height, and viewport-clamped height calculation. The removed `simple-liquid-glass` SVG displacement path is replaced by one stable native CSS surface with the selected 12 px blur and 105% saturation, eliminating filter mount/activation hitches without changing geometry. A golden-yellow point light now sits between the avatar camera and subject inside the avatar-only Three.js scene, so it cannot spill onto the independently rendered map; a restrained matching golden radial highlight at the same screen location carries that illumination onto the CSS liquid-glass surface. Avatar rendering is capped at 30 FPS and 1.5× DPR, suspends when the document is hidden, loads the GLB and face atlas concurrently, and asynchronously warms shaders before revealing the first complete frame. On Retina displays the DPR cap reduces the avatar framebuffer pixel count by about 44% from the earlier 2× path. OS process sampling during development showed two separate Chrome renderer processes near 620 MiB before the pass; after removing SVG glass and reducing avatar GPU work, the same leading processes settled near 366–371 MiB. The 8 MiB avatar GLB itself contains 35,153 vertices plus one 2048×2048 texture and is not large enough to explain the earlier 1.1 GiB total; multiple map-heavy tabs/processes were the dominant contributor.
- Runtime avatar material setup now balances the source GLB's duplicated emissive texture at `0.504`, exactly 20% above the initial `0.42` balanced value, its exported 2× specular response at `0.24`, and the golden front light at intensity `3.2`. The moderate emission softens residual normal transitions without returning the skin to the earlier self-lit white appearance.
- The docked avatar tile is pointer-draggable with mouse, pen, or touch. On release it animates to whichever of the four viewport corners is nearest, mirroring the approved top-right inset: 20 px on desktop and 12 px on the mobile layout. A four-pixel movement threshold separates dragging from activation, so releasing a moved tile does not accidentally reopen the expanded call; ordinary clicks and keyboard activation continue to open it.
- A second unsaved topology-repair candidate is active in the live authoritative `yuyang-avatar-talking-loop-face-v2.blend`: the first pass welded 13,642 exactly coincident, weight-identical split vertices without moving the surface and cleared imported custom split normals. The second pass extends rounded-edge smoothing from 55° to 75° while protecting 8,859 sharper/boundary edges. Recoverable pre-edit and first-pass candidate `.blend` copies are under ignored `data/raw/avatar-mesh-backups-2026-08-21/`. The nearest remaining open-boundary pairs are about 0.09 Blender units apart, so a wider weld would collapse legitimate topology. Remaining visible black flecks were confirmed inside the packed 2048² base-color texture. A generative cleanup candidate was rejected because it returned 1254² output and altered UV islands. The first conservative deterministic pass changed only 1,935 pixels and v2's 11×11 skin-interior median still left longer marks. The active v3 review texture expands the protected skin-island region with a 24-pixel morphological close and uses a 15×15 local median across its interior, changing 410,692 pixels while leaving atlas dimensions, protected UV borders, hair, clothing, and non-skin source pixels in place. This stronger cleaned packed image and the verified three-second idle Action are exported to `public/models/yuyang-avatar-idle-loop-v2.glb` behind a new cache version for site-lighting review. The authoritative Blender source remains unsaved and no animation video has been rerendered pending user approval.
- The future guided agent must always have a first-class camera traversal tool. It will select from the same named, hard-coded camera presets used by the scripted tour and direct landmark interactions, so agent navigation cannot diverge from the site's authored scenes.
- The shared Manhattan/Washington/Union runtime has received a scene-wide CPU and allocation optimization pass: population animations reuse the frame timestamp and a shared rotation axis, high-frequency pointer hit tests are capped at the visible idle frame rate, tile metadata uses pre-indexed lookups, opaque GLB tile materials are no longer cloned solely for arrival animation, prepared tile child transforms are frozen, completed tile-root arrivals are frozen until reused, and teardown deduplicates shared geometry/material disposal. Landmark base-plus-detail meshes remain merged interaction targets.
- The default globe now uses the user-supplied `e54e860710ac4102ad5d57609a5b52b9.glb`, installed under the cache-busting URL `public/models/low-poly-planet-earth-e54e8607.glb`. The asset's eleven authored land/biome/snow meshes and their geographic color placement remain intact; only the water mesh receives the Manhattan powder blue-gray `#7894A0` runtime override. The NYC marker uses this replacement model's conventional geographic axis rather than the retired globe's 90-degree correction. The open Blender comparison scene contains the prior original at X `-1.35`, its palette experiment at X `1.35`, and the new selected site globe at approximately X `4.05` for direct inspection.
- The globe uses map-style controls: mouse/one-touch drag rotates, two-touch centroid movement or two-finger trackpad scrolling orbits in both axes, pinch zooms, and all active interactions render at display refresh rate. The initial globe camera starts at 1,920 units, which is also the maximum zoom-out distance. Attempts to zoom outward beyond that ceiling trigger the same short blocked-zoom pulse used for rejected inward zooms. Its calibrated NYC coordinate faces the camera in a true north-up local frame: latitude runs vertically, longitude runs horizontally, and the New York marker uses a visually corrected +224-degree longitude offset for the imported low-poly coastline. The marker uses the matching east/north/outward tangent basis, eliminating projection roll; its diagonal-to-east elbow is now one continuous polyline, removing the overlapping-mesh seam at the intersection. Globe orbiting tracks longitude yaw independently from a geographic latitude pitch clamped to ±85 degrees, so the pole guard remains at the poles and East Asia can be centered without hitting a misplaced limit. The entire NYC callout group—dot, leader line, and text—is the valid inward-scroll/click destination; invisible padded hit areas make the thin shapes reliable scroll targets without changing their appearance. Clicking New York or scrolling inward over it accelerates the globe camera inward; the cloud veil now begins at 8% and becomes opaque by 38% of the flight, earlier than its prior 25–54% fade. It then cuts invisibly to a close Manhattan camera and pulls back through thinning cloud into the overview. Scrolling outward from Manhattan runs the reverse sequence without remounting the renderer.
- The idle globe now turns eastward at a restrained `0.018` radians per second and pauses that automatic motion during direct dragging and geographic flights. Six scaled copies of the Manhattan passenger-plane model are seeded across both hemispheres, while nine shared passenger boats occupy separate Atlantic, Pacific, Indian Ocean, Caribbean, Mediterranean, and southern-ocean water routes at the approved slower speed. Planes remain four times their first ambient-pass scale, while boats have returned to the approved two-times scale. Inspection of the source GLB bounds found that normalization against outer land geometry places the visible water surface near radius `345`, not the nominal outer `360`; each boat now receives a scale-aware route radius of `345 + 7 × scale`, aligning the bottom of its capsule hull to the ocean surface rather than aligning its object origin. Four low-poly satellites occupy distinct 560–610-unit orbital shells, above the planes at 408 units and the cloud centers below roughly 500 units. Shared templates keep the expanded model count from duplicating geometry and materials. All travelers are children of the interactive globe group, so they remain anchored under manual rotation and transitions.
- Seven deterministic low-poly clouds reuse the Manhattan cloud lobe models while mixing distinct silhouettes, including compact round formations and longer multi-peak clusters. A seeded, jittered golden-angle distribution spreads them across the full sphere at varied altitudes, scales, and tangent rotations. Their height above the globe surface is reduced by 20% from the original distribution. Each cloud moves continuously along its own slow orbital path and reorients every frame so its bottom remains facing Earth; normal depth testing preserves correct front/back occlusion as the globe rotates.
- Branch: `map-preview`.
- Framework: Next.js 16, React 19, TypeScript, and Three.js.
- `/` now renders the neutral `src/components/map/map.tsx` entry through `src/components/map/map-shell.tsx`; the former NYC-specific component names were retired because the persistent renderer now begins at Earth scale and will expand to San Diego and Beijing.
- Manhattan, Washington Square, and Union Square are internal camera states of the single `/` scene. There are no separate routes, query parameters, hashes, or tags. History state changes while the URL remains exactly `/`; a fresh load begins at the Manhattan overview.
- The Manhattan overview uses an expanded 2,600-meter CityGML crop matching the complete 5.2 km ground datum and the same warm deterministic palette as both neighborhood views. Its fixed orthographic composition is centered between Washington and Union and pitched 50° away from straight down so building facades retain substantial visible depth. Manhattan fog is disabled; close-view birds are hidden and the overview cannot be drag-rotated. Washington and Union retain their existing neighborhood fog.
- Manhattan's Hudson and East River edges now come from reproducibly generated OpenStreetMap geometry rather than straight guessed cutoffs. Pier 40 uses its mapped multipolygon footprint and central athletic field without a bespoke high-detail pass. The Hudson tree line is sampled at a consistent 56-meter north/south interval across the complete waterfront instead of following uneven OSM vertex density; its slightly larger trees use the landward shoreline candidate and no longer receive a separate Pier 40 cluster. The East River uses the same dark water treatment and simple mapped piers, supplemented with restrained pier access lanes, waterfront trees, and 67 mapped FDR Drive motorway/primary carriageway segments.
- Washington Square and Union Square remain zoomable views within the same persistent WebGL renderer and Three.js scene. Clicking or scrolling inward over either park from Manhattan enters that neighborhood; scrolling outward returns to Manhattan. Landmark raycasting is disabled at Manhattan scale and remains active within neighborhood views.
- The base Manhattan view now displays two gently floating, raycast-clickable blocky yellow exclamation marks centered directly over Washington Square and Union Square with a half-screen-pixel rightward nudge. Each SVG uses the restored long rectangular bar, circular dot, eight-pixel dark outline, original display size and bob amplitude, plus a layered yellow aura with a strong near halo and broad soft outer bloom. Clicking either mark or beginning an inward scroll over its existing park hit area flies the camera into the corresponding neighborhood; both marks begin fading immediately and disappear over 240 ms. Returning from either neighborhood to Manhattan fades both markers back in over the same 240 ms.
- The Manhattan marker bob compensates for the overview camera's `0.3` orthographic zoom, using a 13.33-world-unit amplitude so its visible screen displacement matches the four-world-unit bob used by the close-view markers at `1×` zoom.
- The SVG destination sprites include explicit intrinsic dimensions and render an immediate matching yellow-exclamation fallback until browser SVG decoding completes, preventing the markers from disappearing during initial load or hot reload while retaining the SVG files as the authored sources.
- The Washington and Union close views reuse the same glowing exclamation SVGs at 25% of the Manhattan display size above every interactive landmark: Lipton Hall, Courant, Stern, and Bobst in Washington; 25 Union Square West and 235 Park Avenue South in Union. These smaller markers share the overview bob cadence with staggered phases and route clicks into each merged building's existing landmark selection interaction. Once any building is selected and the camera tunes in, all close-view markers—including the selected building's marker—fade away; clearing the selection fades them back in.
- Manhattan-to-neighborhood and neighborhood-to-Manhattan camera flights now render at the display's available `requestAnimationFrame` rate instead of inheriting the scene's 30 FPS idle cap. Dragging and blocked-zoom feedback receive the same full-rate treatment, while ambient idle rendering remains capped at 30 FPS.
- Rejected scroll-in over ordinary map space now triggers one immediate 180 ms pointer-anchored pulse per physical wheel/trackpad gesture. The pulse scales from and restores the active view's actual camera zoom, so it works correctly in the base Manhattan overview (`0.3`) as well as neighborhood views (`1`). Subsequent momentum events extend a short gesture-settle window without restarting the animation, eliminating the delayed double-zoom effect.
- `src/components/atlas-map.tsx` has been removed.
- `src/components/map/runtime/map-runtime.tsx` contains the single persistent globe, Manhattan, Washington, and Union scene orchestrator behind the thin `src/components/map/map.tsx` entry.
- The global base now lives at `src/styles/globals.css`; map, loader, avatar, chat, and social-link presentation is split into neighboring feature stylesheets.
- The initial orthographic camera is the fixed Manhattan composition. Washington Square and Union Square retain their north-facing overview presets and horizontal drag rotation after the user zooms into them.
- Lighting now adapts Figma Direction 05 with reduced cool ambient fill, a stronger warm directional key, reduced exposure, firmer soft shadows, and cream haze beginning farther from the foreground to preserve building contrast. Exact token extraction remains blocked by the Figma Starter-plan MCP limit.
- Official planimetric roadbeds use dark brown-gray (`#6F6A61`) to make the street network more legible against the deeper warm buildings and cooler sidewalk datum.
- The top title/instruction overlay has been removed so the map occupies the canvas without a banner.
- A cool gray ground datum sits 0.56 scene units above the dark roadbeds to create a subtle but readable sidewalk curb; authoritative sidewalk polygons remain a later data refinement.
- The WebGL renderer explicitly requests a stencil buffer so the raised sidewalk datum is cut away over official roadbed polygons instead of covering the asphalt.
- Eight low-contrast off-white zebra crossings mark the two adjoining streets at each Washington Square corner. Their centers are verified to fall inside official roadbed polygons so the raised sidewalk mask does not hide them.
- Crosswalks no longer depend on stencil state. Their compact geometry sits at 0.76 scene units, just above the 0.68 sidewalk datum, which guarantees that the paint remains visible over both the asphalt depth and curb edge.
- Lighting remains a single fixed warm daytime treatment; user-local time-of-day lighting has not yet been implemented.
- Nine distinctly shaped bright-white cloud clusters drift east-to-west through staggered lanes in the base Manhattan overview. Closely grouped speeds preserve separation, and off-screen wrap bounds make every formation slide naturally into frame. Clouds and passenger planes render in a dedicated post-city overlay scene, preventing buildings, piers, and water from slicing their geometry. Clouds use a white flat-shaded material under strong neutral hemisphere fill and a gentle directional key, preserving bright color with readable polygon texture. Two western clouds hover above the Hudson at X -1500/-850, Z 850/1050, and Y 950/1150. Current scales range from 2.28 to 2.9.
- Two bright-white passenger jets use separate north–south Manhattan flight corridors, with one northbound and one southbound at different altitudes. Each has a tapered nose, swept wings, twin engines, tail surfaces, cockpit glazing, and cabin windows; both hide automatically on entry into either neighborhood view.
- A small Manhattan-only passenger boat travels slowly northward along the western Hudson. It has a dark low-profile hull, pointed bow, white deck and cabin, dark side windows and windshield, and subtle vertical bobbing above the water surface.
- Sixteen small birds are dispersed across independent overhead lanes, including two near-camera lanes that cover the lower-left projection, with animated wing flaps, gentle vertical drift, varied scale and speed, and seamless off-frame wraparound. No bird geometry or material is created during Manhattan startup; the flock is instantiated lazily only on the first transition into Washington or Union, then retained for later neighborhood switches. White birds use the original long-winged silhouette; gray birds use a distinct pigeon model.
- A deterministic ambient population adds 600 low-detail instanced pedestrians across the Washington and Union neighborhood areas. Each view receives 300 candidates, and every complete circular walking route is sampled with clearance probes against roads, CityGML building footprints, the fountain, and park-tree footprints before acceptance. Walkers load once but remain hidden and skip animation work in Manhattan; they become visible only in the two zoomed-in neighborhood views.
- A 600-vehicle low-detail instanced fleet keeps traffic distributed across the expanded Manhattan overview. Cars use verified straight routes along the principal axes of elongated official roadbed polygons. Center and side probes across every route remain within the source road polygon, outside holes, and clear of CityGML footprints; car geometry is aligned to its travel vector. The fleet samples evenly across the full spatially ordered route list, reverses continuously at validated endpoints, and keeps exactly every third vehicle taxi yellow.
- Bobst Library, Courant, the grouped Stern pair, and windowed Lipton Hall are discoverable interactive landmarks. Edge outlines have been removed entirely and replaced by an animated enchantment-style surface glow that cycles violet-to-cyan and pulses in intensity, becoming stronger on hover or selection. Landmark glow is hidden throughout the base Manhattan overview and appears only in the Washington Square or Union Square neighborhood views. Hover raises the complete merged target 4.5 scene units, but selection grounds it and eases into a substantially closer (`2.41x`, about 40% above the former `1.72x`) fixed view for every landmark. Courant uses a north-facing azimuth (`0`) with its target shifted 10 units west and 24 units south to frame Courant Plaza and Gould Plaza together. Stern uses the requested southwest-facing azimuth (`Math.PI / 2 + 0.42`) and has a dedicated camera height of `252`, 20% above the other non-Lipton landmark height of `210`. Bobst retains its south/front-side azimuth (`Math.PI - 0.42`), and Lipton retains its approved western-facing azimuth (`0`) and height `273`. Scroll-in over a hovered landmark triggers selection. Scroll-in elsewhere runs a subtle 180 ms `1.035x` rejection pulse anchored toward the pointer's ground position and returns exactly to the overview; trackpad momentum is coalesced into the active gesture so it cannot launch a second pulse. Escape or scroll-out exits selection, restores the containing Washington Square or Union Square overview, and unlocks rotation. Positive wheel/trackpad momentum is coalesced so that same gesture cannot continue outward into Manhattan; returning from the neighborhood to Manhattan requires a separate scroll-out gesture. Courant, Stern, and Lipton's merged CityGML shells render double-sided so source faces remain visible regardless of winding after the base and higher-LOD geometry are baked together.
- Landmark glow is exclusive during selection: the selected target keeps its animated violet/cyan pulse while every other selectable landmark glow fades completely to zero until Escape or scroll-out clears selection, at which point their idle glow resumes.
- Only windowed Lipton Hall BIN `1008875` receives the interactive treatment. Its authoritative base shell and 447 windows are baked into one mesh, its bottom/ground surface is gray, and its BIN is omitted from regenerated background tiles to prevent duplicate geometry. The adjacent tower remains ordinary context geometry.
- Two exact Union-area addresses now use the merged interactive-landmark treatment. BIN `1087304` (25 Union Square West) receives tan walls, a muted sage-green authoritative roof, dark facade windows, and a close-view preset whose horizontal azimuth is unchanged while its elevation is pitched 15° higher than the original view. BIN `1017906` (235–237 Park Avenue South / 101–103 East 19th Street, home of Union Square Cafe) uses clearly lighter warm-tan masonry (`#C7AA83`), a subdued gray-brown roof, dark facade windows, and a taller close-view preset at azimuth `-5π/12`, exactly 30° clockwise from its original southwest `-π/4` preset. Each base shell and all generated windows are baked into one double-sided selectable mesh before glow, raycasting, hover lift, or camera selection; both BINs are omitted from regenerated background tiles to prevent duplicates. The previously mistaken BINs `1016075` and `1078701` are ordinary context buildings again.
- Clearing a landmark selection is now context-aware: Escape or scroll-out while the active destination is Union Square restores the Union overview centered at local `(545, 28, -580)`; Washington landmark exits still restore the Washington overview.
- Landmark detail generation now skips rooftop-equipment construction for IDs without an explicit reviewed equipment layout. This prevents the two Union Square West window-only landmarks from passing an undefined placement list into the HVAC loop and aborting the complete scene load. Destination-triggered neighborhood loads also attach immediate rejection handling so expected aborts during React/HMR cleanup cannot become unhandled fire-and-forget promise rejections.
- Pedestrian and traffic route validation now uses a 64-meter spatial footprint index instead of scanning roughly 3,000 CityGML footprints for every clearance probe. This removes the multi-billion-check client startup path that could freeze the page and cause severe allocation pressure.
- Structured `[WashingtonSquare load]` client logs report elapsed time, payload sizes, dataset counts, geometry phases, Arch/Bobst GLB completion, and load failures. The first four-tile decode batch and deferred pedestrian/traffic/path construction now emit explicit start/end pairs; end records include `stageDurationMs` so total startup elapsed time is no longer mistaken for the cost of an individual stage. Three.js `Timer` and `PCFShadowMap` replace the deprecated `Clock` and `PCFSoftShadowMap` APIs.
- Runtime CityGML parsing and triangulation have been removed. `scripts/map-data/build-washington-city-tiles.mjs` preprocesses a shared 1,250-meter crop of 8,874 source CityGML buildings into 186 spatial, front-face-normalized, Meshopt-compressed GLB tiles plus a compact runtime footprint/detail file. Manhattan loads the full crop progressively; Washington and Union filter it to their respective 900-meter neighborhoods.
- Tile URLs carry a content version derived from both the CityGML source and generator code, preventing immutable-cache staleness after palette or geometry changes. Tile GLBs receive one-year immutable cache headers; metadata is revalidated.
- Chrome rendering now starts at up to 2× device pixel ratio (1.5× on lower-core devices) and never adaptively drops below 1.5× where supported. Active globe input bypasses the 30 FPS idle cap; static scenes remain throttled, and rendering still suspends entirely when hidden or off-screen.
- The static 1024 city shadow map is now demand-rendered instead of being regenerated on every 30 FPS ambient frame; it refreshes only as tiles/custom models arrive or while a hover-lifted landmark physically moves. Landmark raycasting also reuses a stable root array and direct root lookup instead of allocating and linearly searching on every pointer event.
- Nearest-first city-tile downloads now begin before synchronous pedestrian, traffic, and landmark construction, overlapping network/decode with startup CPU work. Stable `/data/*.json` assets receive a one-day browser cache with one-week stale-while-revalidate; versioned tile GLBs retain one-year immutable caching.
- Neighborhood loading is radial from the active camera destination, runs in four-tile decode batches, and cancels superseded radial queues when the user switches squares. Roads use the exact same 34-unit rise, fade duration, damping, and overshoot as building tiles, but start first; then all buildings batched within a spatial tile rise together while successive tiles stagger outward from the frame center. Retained destination tiles replay the entrance on traversal, while tiles beyond 700 meters of the settled destination are removed and disposed so the inactive neighborhood is not kept resident.
- The square switcher is a raycast-clickable world-space arrow mesh. Its Washington position is local `(420, -175)`, placing it over the upper block indicated in the latest Washington reference; its Union position is `(125, -345)`, placing it 60 world units farther south over the lower red-tower area. The 1.65× extruded/beveled body now uses warm light gray `#E2DFD6`, low white emissive intensity `0.035`, and roughness `0.68`, making its face angles, bevel shading, thickness, and cast/received shadows readable at both positions. Its slightly oversized additive shine shell is limited to `0.07` opacity so it no longer washes out the geometry. The arrow stays hidden through all other startup layers, then rises and scales through a 1,120 ms spring after the birds are created, making it the final scene entrance; afterward it resumes its subtle bob.
- Building and custom-building arrival materials remain fully opaque throughout their spring rise. Waiting roots remain completely hidden until their reveal timestamp so tall special-color buildings cannot protrude early. Only the gray ground datum fades; roadbeds remain solid during their spring.
- Startup overlaps network work but preserves strict visible staging: the gray datum begins its 220 ms fade on the first animated frame, independently of network work. Meanwhile the 854 KiB Manhattan road GLB and center building tiles load/decode; roads spring first once ready and complete their full 1.12-second entrance, then the first building ring is released after a 50 ms handoff. Custom buildings and birds remain later stages.
- The server-rendered map shell now starts its matching 220 ms white-to-gray datum fade before React or WebGL initializes, and the Three.js clear color matches that final gray. This hides JavaScript startup and prevents a white-frame reset when the canvas takes over.
- The discarded world-space survey grid, tile-corner markers, `NOW BUILDING` label, and perimeter progress indicator have been removed.
- The independently started road GLB promise attaches AbortError handling immediately, so normal React cleanup/navigation aborts cannot surface as unhandled promise rejections before the staged loader reaches its later await.
- The road GLB remains preloaded from the server-rendered document head, but there is no longer a loading veil or complete-scene release gate.
- The former 1.45-second and 1.7-second visual-staging waits were removed because they served no purpose behind the full-scene loading gate and added 3.15 seconds to startup.
- The full-screen determinate loading prelude and its canvas opacity gate have now been removed. The server/dynamic fallback immediately shows the matching gray base datum, and the WebGL canvas becomes visible as soon as it mounts instead of waiting for the complete scene.
- Scene preparation is parallel while visual reveal remains ordered. Road GLB loading, city-tile requests/decoding, and ambient construction overlap; decoded buildings remain hidden until the road spring has completed, and ambient layers remain hidden until the complete building/custom-landmark phase has completed.
- The visible startup sequence is now a whole-object spring rise for the base datum, then whole-object drop-and-settle springs for the Hudson/East River surfaces plus springing Pier 40/mapped East River piers, roads, radial building tiles/custom landmarks, park grass/paths/trees/fountain, staggered cars/pedestrians, clouds/planes/birds, and finally the navigation arrow. Water drops from above because an upward spring would remain occluded beneath the opaque datum until its final frame. Water geometry is never horizontally scaled, so the base cannot appear to stretch across a temporarily unrevealed river footprint. Instanced cars and pedestrians rise individually in a deterministic dispersed stagger rather than appearing as complete fleets in one frame.
- All mapped FDR Drive carriageway meshes are parented under one roadway group before staging, so every lane enters with one shared spring transform instead of populating as separate line fragments.
- The common spring-arrival helper preserves each object's authored destination elevation. This keeps clouds and planes at their correct initial depth/elevation when first revealed instead of temporarily moving them toward world `y=0` and relying on a later ambient-animation pass to repair the position.
- React Strict Mode is disabled in `next.config.ts` for this imperative WebGL prototype. Its development-only effect remount was creating and disposing the complete renderer/scene twice, producing duplicate load logs and a visible river/canvas flash now that no loading veil hides the first teardown. Production rendering behavior is unchanged.
- The gray land datum is visible in the first rendered frame. After a 260 ms land beat, both complete shaded river surfaces fade in together over 260 ms at their final Y `0.92`; there is no directional fill, vertical travel, shoreline shift, land-plane crossing, or spring overshoot. The experimental lowered riverbed and stencil cutouts remain removed. Piers, roads, buildings, parks, and ambient details are held until the water fade completes. The canvas is appended only after an offscreen first-land render.
- The former 220 ms white-to-background CSS startup animation is removed completely. The canvas also has a CSS water-color backing and the renderer explicitly clears its drawing buffer to `#354345` before appending it, preventing either the HTML shell or an unrendered WebGL buffer from flashing white/black during handoff.
- Background-tab suspension now covers the loading pipeline as well as rendering: radial tile batches and expensive route/landmark construction wait for a visible document before scheduling more work. Initial tile decode concurrency is reduced from 12 to four with a main-thread yield between batches, and the redundant 12-request raw center-tile warm-up has been removed.
- Abort detection now accepts any error-like value named `AbortError` rather than requiring a `DOMException`, preventing normal refresh, Strict Mode teardown, and dynamic-remount cancellation from surfacing as unhandled promise rejections.
- The initial radial neighborhood promise installs its rejection handler at creation time and records non-abort failures for the later awaited error path. Browser-managed response/body promises still surfaced outside that chain when the shared signal was canceled, so React/HMR cleanup now marks the scene disposed without aborting in-flight requests; every async branch already checks `disposed` before mounting decoded results.
- Every radial tile decode absorbs normal `AbortError`s immediately at its source. Tile loading-set cleanup runs in `finally`, so React Strict Mode teardown, navigation, or unmount cannot leave a tile permanently marked in flight.
- The 12 nearest GLB tile requests begin as soon as the manifest response arrives, avoiding the network contention caused by the former 24-request warm-up. The browser no longer fetches or parses source planimetrics; road mounting uses the compiled GLB and ambient objects use build-time frozen data.
- Tile cleanup callbacks are destination-generation guarded, so a delayed cleanup from the previous view cannot delete tiles after the camera returns. Neighborhood views retain a 900-meter radius; Manhattan requests the complete crop. Disposed arrivals are removed from the animation queue before GPU resources are released.
- All runtime data and GLB requests use one shared signal, while teardown safety relies on `disposed` guards rather than canceling that signal. Full scene geometry/material disposal remains in cleanup. The first meaningful city-tile batch triggers a 900 ms map fade-in with a reduced-motion fallback.
- `public/data/washington-square-citygml.json` is the tracked 66.17 MiB shared crop of official NYC CityGML delivery area 12: 17,373 buildings, 407,393 surfaces, and 2,033,429 vertices within 2,600 meters of Washington Square. Its 484 Meshopt-compressed spatial GLB tiles total about 15.6 MiB and fill the complete overview datum while preserving radial loading.
- `public/data/manhattan-planimetrics.json` contains 3,644 tracked roadbed polygons from the official NYC 2022 Planimetric Database across the complete 2,600-meter/5.2 km Manhattan datum. The source query limit is 50,000 so perimeter features cannot be silently truncated.
- `scripts/map-data/build-washington-road-glb.mjs` pretriangulates those roadbeds into the tracked 1.2 MiB Meshopt-compressed `public/models/manhattan-roads.glb`. Its versioned URL prevents the former immutable road asset from remaining in browser cache.
- Ambient spatial validation has moved completely out of the browser. `npm run data:ambient-layout` validates deterministic source data once and writes tracked `src/generated/ambient-layout.ts`, currently freezing 624 tree transforms across 69 rendered green/recreation areas, 600 pedestrian circular routes, 600 vehicle routes, and 919 park-contained/road-cleared detailed path segments. Every grass area rendered from this audit has at least one boundary-, path-, road-, building-, and globally spacing-cleared tree.
- `public/data/manhattan-parks.json` is the reproducible OpenStreetMap audit source for Manhattan context green/recreation rings and pedestrian paths. It includes mapped parks, gardens, playgrounds, pitches, recreation grounds, grass, and village greens so block-scale recreational voids are populated. `npm run data:manhattan-parks` regenerates it; Washington Square, Union Square, and Gramercy retain their dedicated higher-detail sources.
- Tompkins Square Park, both halves of Stuyvesant Square, and Jefferson Market Garden are fetched with their complete OSM topology because their features straddle the audit's tiled map exports. The frozen result includes 60 Tompkins trees, 23 Stuyvesant trees, and one tree inside Jefferson Market Garden's narrow `15.8×58.3`-meter footprint.
- The West Village cross-cell set also pins complete topology for Christopher Park, Sheridan Square Garden, Jackson Square Park, and the NYC AIDS Memorial Park at St. Vincent's Triangle. Their frozen populations are one, one, one, and two accepted trees respectively; Christopher's corrected full ring has 16 points instead of the incomplete five-point export.
- `West 11th Street Faux Park` is the sole authored non-OSM park. Its four corners are derived from the actual West 11th/West 12th and Sixth/Seventh Avenue South intersections immediately east of the AIDS Memorial, then inset eight meters from the roads. It contains 33 build-validated trees.
- `public/data/washington-square-addresses.json` joins current NYC Property Address Directory 26B records to CityGML buildings by BIN: 8,530 matched BINs and 11,763 unique addresses.
- `public/data/union-square-park.json` tracks 392 Union/Gramercy context paths and four park polygons from OpenStreetMap, including the verified Gramercy Park ring centered near local `(970, -791)`. `scripts/map-data/build-union-square-park.mjs` regenerates it against the shared Washington-origin coordinate system. Gramercy appears only as Union-side context.
- Expanded planimetric and park JSON requests carry an explicit shared-data query version so browsers cannot combine the enlarged CityGML tiles with the former cached road/park crop.
- Every mesh in the precompiled road GLB renders at order `-1`, restoring the stencil-first contract before the gray datum and preventing irregular white background cutouts around Union Square.
- Complete downloads and temporary conversion files are gitignored under `data/raw/` and `data/work/`; only cropped runtime data is tracked.
- Repeatable data scripts are `scripts/map-data/extract-washington-citygml.py`, `scripts/map-data/build-washington-address-index.py`, and `scripts/map-data/build-washington-planimetrics.mjs`.
- `scripts/map-data/build-washington-park.mjs` reproducibly crops OSM park paths, the fountain, and Washington Square Arch footprint into tracked `public/data/washington-square-park.json` under the ODbL.
- `TODO.md` records later prototype cleanup, a future indexable personal blog, and a layered background-audio system with music plus camera-state ambience: birds and airplanes over Manhattan, people talking in close views, and stylized satellite/space sounds at Earth scale.
- Tripo Smart Low-Poly task `c9929452-f87b-4b89-97c6-e99568f1d230` successfully processed the untouched native Meshy flat-face model with baking, quad output, and a 50,000-face ceiling. The returned FBX contains one unrigged mesh with 11,855 vertices and 12,764 polygons; its mesh-preserving Blender GLB conversion is `public/models/yuyang-avatar-flat-face-tripo-quad-50k-review.glb`, with Tripo's preview under `public/style-references/avatar/`. This remains a review candidate and has not replaced or been bound to the authoritative 54-bone skeleton.
- The user approved the Tripo surface for rigging. `assets/blender/yuyang-avatar-tripo-quad-50k-rig-review.blend` now binds its original connected quad FBX mesh—not the glTF-expanded intermediate—to a copy of the definitive 54-bone V2 hierarchy. All 54 matching deform groups cover every vertex; all 30 finger bones and both retained idle/talking Actions survive the inspection GLB round trip; frames 1/89 have `0.0` maximum vertex delta, and frames 1, 23, 45, 67, and 89 remain finite. A targeted central-torso cleanup removed false arm-chain influence from 459 sweater vertices caused by nearest-surface transfer in the arms-down pose. The candidate and eight-view contact sheet are ready for visual deformation inspection but have not replaced the authoritative source or live avatar.
- User inspection correctly found that the candidate's arms do not match the authored idle pose. The x-ray bind overlay at `public/style-references/avatar/yuyang-avatar-tripo-v2-skeleton-front-overlay.png` confirms why: hips, legs, torso, shoulders, elbows, and wrists broadly occupy the mesh, but both five-digit rest chains are displaced laterally outside the modeled hands. The candidate is therefore structurally complete but not a correct final fit; its rest-bone hand/finger positions and corresponding weights must be refitted before approval.
- Meshy AI Rigging task `01a02667-642a-7720-8a81-c96699b37a2d` then processed the approved untouched Tripo GLB for 5 credits. Front and side x-rays confirm that its 24-bone AI skeleton fits the sloped shoulders, arms, elbows, wrists, torso, hips, legs, ankles, neck, and head substantially better than the rejected direct V2 transfer. Meshy does not provide finger bones, so this output is an inspection/placement guide rather than the final definitive 54-bone avatar. Its preserved review GLB and x-ray are under `public/models/` and `public/style-references/avatar/` respectively.
- Tripo riggability task `482eb07d-3b64-419c-b09c-b9e0513ba85d` classified the approved surface as a biped, and Mixamo-spec rig task `d46ab2d2-015a-4564-a8e0-acc4fcabc073` returned a 52-bone rig with all 30 finger bones and complete weight coverage. A hybrid review source now keeps Meshy's fitted 24-bone body and body weights while attaching only Tripo's digit rest chains and localized weights to Meshy's `LeftHand`/`RightHand`, yielding the required 54 bones and 54 groups with zero unweighted vertices. The first transfer attempt was automatically rejected after its coverage audit found 23,266 zero-weight vertices; the corrected isolated-copy transfer preserves Meshy's body groups and passes a finite 30-bone curl evaluation. Full-body and bilateral hand x-rays are ready for visual review; the hybrid remains non-authoritative.
- The authoritative `Yuyang_Idle_Loop_v2` and `Yuyang_Talking_Loop_v2` Actions are now attached to the hybrid 54-bone review rig. Meshy's placeholder clip was removed. The idle retains frames 1–76 and closes within `7.45e-09` maximum bone-matrix delta; talking retains frames 1–89 and closes at exactly `0.0`. Both remain finite across five sampled deformation frames. Blender persistence was explicitly fixed by retaining both Actions as data users, and the combined GLB round-trips with both names and all 54 bones. The eight-view animation contact sheet is ready for visual approval; nothing is deployed yet.
- `public/models/yuyang-avatar-hybrid-meshy-body-tripo-hands-talking-review.glb` is the dedicated talking-only export. A clean Blender round trip confirms all 54 bones and exactly one Action, `Yuyang_Talking_Loop_v2`.
- The upstream `norio/vrm-game-starter` repository is cloned separately at `../vrm-game-starter` at commit `b14c236fd8150855348ad085b7820c298eac4b30`. Its byte-identical 46-clip `AnimationLibrary.glb` is preserved locally as `public/models/quaternius-vrm-animation-library.glb`; it is derived from Quaternius's Universal Animation Library and retains that asset's separate license. A first world-space/name-mapped runtime test against the current non-normalized Yuyang rig was visually rejected because it raised both arms overhead. The live loader was restored to the previous embedded idle and passes ESLint/TypeScript. The library must only be connected after the avatar is converted to a normalized VRM humanoid target and the upstream VRM retargeter can be used as designed.
- `public/models/yuyang-avatar-vrm1-review.glb` is now the standardized direct-rig review candidate. A reproducible Node script adds VRM 1.0 metadata and standard names to Tripo's fitted 52-bone full-finger hierarchy without modifying geometry, weights, textures, or inverse bind matrices. The idle loader uses `@pixiv/three-vrm` 3.5.3 plus the upstream VRM Game Starter world-space retargeter for `Idle_Loop`. The first site render exposed inherited old-rig camera and face coordinates: the body sat below frame and the face floated above it. The shared renderer now derives framing and face placement from the new GLB's measured one-meter bounds. Per user direction this correction was verified by GLB structure, targeted ESLint, and full TypeScript checking without another browser visual pass.
- Follow-up site review confirms the VRM avatar is now fully framed and its face follows the head, but the first bounds-relative face was too large. The face canvas is reduced from `0.205 × 0.255` to `0.15 × 0.186` times measured avatar height while retaining the accepted alignment. `Idle_Loop` is connected at runtime from the separate library through the VRM retargeter and `AnimationMixer`; it is not embedded in the avatar GLB.
- `public/models/yuyang-avatar-vrm1-idle-embedded-review.glb` now embeds the retargeted three-second `Idle_Loop` for inspection in an ordinary GLB viewer. The first embedded review revealed that only the upper limbs were side-swapped; the conversion now maps Tripo's negative-X shoulder/arm/hand/finger chain to VRM left and its positive-X chain to VRM right while leaving the accepted body and legs unchanged. The regenerated GLB preserves one mesh, one skin, all 52 VRM humanoid bones, 53 animation channels, nonzero `0.0693353` maximum motion from the first sample, finite values, and exact `0.0` loop closure.
- Further matched-frame inspection proved the arm/finger failure was not just side naming: absolute VRM retargeting imposed the library's T-pose assumptions on Tripo's non-normalized arms-down bind, placing target hands above the mapped spine. The embedded baker now preserves every fitted target rest transform and transfers only source motion deltas conjugated through each source/target rest-axis difference, including all 30 finger bones. At the audited middle frame, target hands and index fingertips are below the chest as anatomically expected. The final file has 53 channels, `0.0752035` nonzero maximum motion, finite values, and forced `0.0` loop closure. The website now plays this exact embedded GLB directly instead of retargeting the library again at runtime.
- Follow-up review found a small right-pinky deformation; all three `rightLittle` joints are now held at their fitted neutral transforms for this idle, while every other digit retains delta motion. The user also found the full-body sway too strong and correctly noted that the delta-only result does not reproduce the source's absolute pose. `public/models/quaternius-idle-loop-reference.glb` now isolates the untouched original mannequin and its sole three-second idle Action for direct comparison. Exact source-pose reproduction still requires rebinding the avatar mesh to the library's actual T-pose skeleton; the current safe delta method intentionally preserves the Tripo arms-down bind.
- The first isolated source-reference export was rejected because Blender re-export reduced the visible result to an incorrect square. It has been replaced byte-structurally from the original library: the corrected `quaternius-idle-loop-reference.glb` preserves the exact `Mannequin` scene (`8,546` uploaded vertices, `13,744` triangles, `1.829 m` height) and retains only the original `Idle_Loop` Action (`159` channels, three seconds).
- At user request, the earlier absolute-retarget checkpoint seen before the arm-side correction has been reconstructed rather than inferred from later files. `yuyang-avatar-vrm1-arm-swapped-recovery-rig.glb` restores the original VRM upper-limb assignments, and `yuyang-avatar-vrm1-idle-arm-swapped-recovery.glb` restores the absolute VRM Game Starter retarget with one three-second, 53-channel `Idle_Loop`. These recovery assets remain separate from the current site GLB.
- `yuyang-avatar-vrm1-idle-arm-corrected-review.glb` derives directly from that recovered behavior and swaps only the complete left/right shoulder, arm, hand, and 30 finger assignments. Its body, legs, three-second timing, and 53-channel absolute idle retarget are otherwise unchanged. The corresponding rig-only file is `yuyang-avatar-vrm1-arm-corrected-rig.glb`; neither file is deployed pending inspection.
- User inspection found that swapping the bind assignments drove the arms into a T-pose and left a pinky malformed. A follow-up diagnostic kept the recovered bind and exchanged only corresponding upper-limb animation channel targets. Its frame-45 full-body x-ray at `yuyang-avatar-vrm1-idle-channel-swapped-skeleton-xray.png` shows the shoulder/clavicle chains crossing through the chest, proving the mirrored local axes require a proper quaternion-basis conversion rather than either metadata or channel swapping. This candidate is rejected and not deployed.
- Further arm-only mapping diagnostics eliminated opposite-side transfer with and without sagittal reflection, same-side reflection, recovered target-space delta mirroring, and complete pose mirroring: these variants either restore the source T-pose, invert fingers, or retain crossed clavicles. `yuyang-avatar-vrm1-idle-arm-hand-adapter-review.glb` now keeps the recovered hips, legs, torso, neck, and head exactly as animated while replacing only shoulders, arms, hands, and all 30 finger tracks with the fitted-rest axis-conjugated limb clip. Its midpoint x-ray shows both complete arm chains inside the mesh without crossing; the file remains an isolated three-second, 53-channel review candidate and is not deployed.
- User inspection correctly rejected that first adapter: it fell back to arms-down motion and retained long cross-body hand spikes. A deformation audit found the concrete skinning cause—87 spatially right-hand vertices carried opposite-side `LeftPinky3` influence, reaching 43% on individual vertices. Those influences are removed and renormalized in `yuyang-avatar-vrm1-idle-arm-hand-weight-fixed-review.glb`. The current `yuyang-avatar-vrm1-idle-direction-arms-weight-fixed-review.glb` additionally replaces only shoulder, upper-arm, forearm, and hand rotations with physical joint-direction retargeting while preserving the accepted body and finger tracks. Its midpoint x-ray shows uncrossed arms with bent elbows beside the hips, and the 25-frame audit reduces the former maximum cross-hand stretched edge from about `0.226 m` to `0.032 m`. It is not deployed pending direct user inspection.
- The user requested that hands return to the original recovered animation rather than the direction-mapped hand pose. `public/animations/idle.glb` now preserves both original hand bones and all 30 original finger tracks, retains the 87-vertex cross-hand weight cleanup, and direction-retargets only shoulders, upper arms, and forearms. The shoulder solve uses the target torso's animated world transform at each frame instead of its rest transform, removing the former behind-the-torso elbow drift. Its midpoint x-ray shows symmetric uncrossed arms and both elbows forward/down. The candidate remains undeployed pending GLB inspection.
- The accepted arm-mapping state is now frozen as the starting checkpoint for small adjustments. The complete fresh-session recovery contract is in `docs/avatar-animation-system.md`: it records the exact VRM Game Starter commit and local Quaternius library/reference files, distinguishes permanent skeleton mapping from clip-specific tuning, preserves target bind lengths/rest transforms and original hand/finger tracks, requires animated-parent world-space arm solving, and mandates direct GLB, skeleton X-ray, loop, hierarchy, and deformation audits. Six obsolete avatar review `.blend`/`.blend1` files were removed from `assets/blender/` and placed in the ignored recovery archive; only the authoritative avatar Blender source remains there.
- The obsolete atlas implementation, pins, separate routes, and remounting view architecture remain absent; the new Manhattan overview and its transitions are camera states inside the current persistent scene.
- ESLint, TypeScript, and the optimized production build pass after moving ambient validation to the build-time generated layout. The production route remains statically prerendered.
- The working tree contains uncommitted prototype files.

## Next implementation milestone

1. Review `yuyang-avatar-idle-loop-v2.mp4` and `yuyang-avatar-talking-loop-v2.mp4`; if approved, wire their single Actions into the avatar call shell and crossfade between them over roughly 0.25–0.4 seconds. Author wave/thumbs-up clips separately on the same 54-bone skeleton.
2. Visually approve the live idle GLB, planted-foot behavior, docked waist-up framing, and Head-bound smiling face in the FaceTime shell. Then implement the same atlas-state mechanism for talking and crossfade between the idle/talking skeleton Actions over roughly 0.25–0.4 seconds.
2. Visually tune the minimized CSS crop manually against the shared `0.93` full-screen camera; the dock no longer owns a separate Three.js camera.
2. Visually review the warm, rotatable CityGML and planimetric roadbed scene, including the scale, visibility, density, and speed of the dispersed white birds.
2. Visually review the new OSM hardscape alignment and tune path widths/material contrast without adding greenery yet.
3. Visually review Courant, Stern, the rotunda flag/glass, large rooftop fans, and Gould Plaza at both overview and the planned close zoom; tune coordinates only from that review.
4. Visually verify corrected Bobst scale, position, and street-edge rotation in the live map.
5. Refine the new warm material, shadow, road contrast, and background treatment from user feedback.
6. Visually review the two new Union Square West landmark palettes, generated window density, sloped green roof, and fixed camera framing in a live browser; tune only from that review.
7. Expand beyond Washington Square only after this small study is approved.
8. When the multi-scale site shell resumes, implement the persistent asset lifecycle (`downloaded` → `prepared` → `visible`), destination-prioritized preload queue, memory-budgeted prepared cache, and camera-transition reveal contract described above before adding more geographic levels.
9. Extend the blank first-load call shell into the conversational intro only after the replacement avatar direction is approved. Add the scripted tour state machine and expose named camera presets through a permanent camera-traversal tool shared by guided and freeform agent interactions.
10. Extend the initial agent command bus from geographic views to the existing named landmark camera presets, connect approved avatar one-shots as consumers of `trigger_avatar_emote`, and replace the per-instance rate guard with a distributed limiter before public production traffic.
