# Milestone state

`current.md` is the authoritative rolling project state. It currently records the avatar and map status, including the checksum-verified gitignored avatar working snapshot, rejected voxel-avatar V1–V3 iterations and the current unapproved illustrated-face voxel-avatar V4, the literally centered fullscreen modal with its bounded 82.08-rem maximum width and restored earlier avatar camera offset, the 170-character segmented agent-turn ceiling and presentation pills, the arrival-gated Washington-to-Lipton guided-tour sequence, birthday-derived intro age, the arrival-gated Manhattan tour greeting, per-turn date/time context, persistent dismissible streamed captions beside the docked avatar, the shared live agent/map camera-view registry, fully settled multi-level camera chains, direct scroll transfer between authored landmark views, the manually fitted and locked GLB face, five-second fixed-leg idle, the first unapproved idle-based wave candidate, chin-up gaze, and revised full-screen framing, plus validation and pending production hardening. Update it after meaningful project work with decisions, blockers, and next steps.

Append-only session handoffs live in `history/`; never revise an old handoff to describe later work.
