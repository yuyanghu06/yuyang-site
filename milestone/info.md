# Milestone state

`current.md` is the authoritative rolling project state. It currently records the shared full-screen FaceTime avatar camera and state-specific animation structure alongside the broader map and avatar status. Update it after meaningful project work with decisions, blockers, verification, and next steps.

Append-only session handoffs live in `history/`; never revise an old handoff to describe later work.
