# Agent memory

This folder owns private retrieval from the canonical Pinecone memory store.

## Direct subfolders

There are no direct subfolders.

## Direct files

- `INFO.md` documents this folder.
- `retrieval.ts` performs conversation-aware embeddings, parallel dense and metadata-filtered Pinecone searches, reciprocal-rank fusion, and evidence formatting.
