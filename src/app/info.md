# App Router entry

This directory owns the root App Router layout, page, metadata/icon, and global CSS. The root page mounts the browser-only map shell.

Before changing App Router conventions, read the relevant installed Next.js documentation under `node_modules/next/dist/docs/`.
