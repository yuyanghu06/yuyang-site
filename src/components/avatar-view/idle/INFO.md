# Idle avatar view

This folder contains the idle-specific implementation for the shared full-screen avatar renderer.

## Files

- `idle.tsx` loads the authoritative idle GLB, creates its fixed-body animation clip and mixer, and supplies them to the shared `avatar-fullscreen.tsx` renderer.
- `INFO.md` documents this folder.
