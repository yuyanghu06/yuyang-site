# Map-data pipeline

These scripts generate tracked road, park, waterfront, ambient-layout, address, and CityGML runtime assets.

Preserve deterministic placement, shared coordinates, road/footprint validation, content versioning, and merged landmark geometry. Run only the targeted generator needed during iteration.
