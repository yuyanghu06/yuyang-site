# Build loaders

This folder contains small build-time source loaders used by both Turbopack and webpack.

## Subfolders

This folder has no direct subfolders.

## Files

- `markdown-string-loader.cjs` converts an imported Markdown file into a trimmed JavaScript string module so dialogue remains authored in Markdown without runtime file fetching.
