# Runtime map data

Tracked JSON datasets consumed by the browser live here. Most are reproducible outputs of `scripts/map-data/`.

Preserve the shared Washington-origin coordinate system and version/cache contracts when regenerating related datasets.
